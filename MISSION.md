# Mission: 내 Kaggle 파이프라인의 모델링 기법을 스스로 판단할 수 있게 이해하기

## Why
Playground S6E8 대회를 진행하는 이 저장소의 파이프라인에는 fold 설계, target encoding, Lookup-Transformer 같은 기법이 이미 들어와 있다.
공개 노트북을 재현하는 데서 멈추지 않고, 각 기법이 왜 이 데이터에서 통하는지 원리를 이해해 다음 실험을 스스로 설계하고 판단할 수 있게 되는 것이 목표다.

## Success looks like
- 파이프라인의 구성 요소(예: inner 10-fold, lookup embedding)를 남에게 근거와 함께 설명할 수 있다.
- 새 실험 아이디어를 보고 이 데이터의 성질(합성 데이터, 정확값 반복, 결측)에 비추어 통할지 사전에 추론할 수 있다.
- 공개 노트북의 기법을 가져올 때 무엇을 그대로 두고 무엇을 프로젝트 규율에 맞게 바꿔야 하는지 판단할 수 있다.

## Constraints
- 수업은 짧게(10분 이내), 이 저장소의 실제 코드와 실험 결과에 근거를 둔다.
- 수업 본문은 한국어, matplotlib 차트를 만들 때는 텍스트를 영문으로만 쓴다.

## Out of scope
- 대회와 무관한 일반 딥러닝 이론(예: LLM, vision)은 다루지 않는다.

<!-- 이 미션은 대화 맥락에서 추정해 작성한 초안이다. 다르게 생각한다면 알려 주면 수정한다. -->
