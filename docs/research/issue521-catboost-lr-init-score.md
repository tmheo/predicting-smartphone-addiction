# 이슈 521: CatBoost 정확값 선형 OOF 로짓 초기 점수 판정

## 결론

`exp210_issue520_cat_lr_onehot_init`은 난수 42 짝비교와 진입 하한, 중복 관문을 통과했지만 `candidate-pool-v2` 핵심 결합 방식 판정에서 음의 기여가 나와 기각했다.
후보 풀과 `artifacts/pool.yaml`은 변경하지 않았다.

## 실행 정체성

- 커밋: `bf0fd22fd1fe541fd969e7526161da61b68887d0`
- 실행 환경: 로컬 CPU
- 기준 난수 42 실행: `50613337010a48efbf771be90bfa636b`
- 후보 난수 42 실행: `74d7f8a6aedb4dc2a2e3d0d94ea1fc75`
- 후보 난수 42, 43, 44 확정 실행: `26f012284cfc40948faeca7cbb9bb06e`
- 기록된 기준 3시드 실행: `521b4924b2ef4ba1830724333b83deb2`

세 정식 실행은 메인 MLflow에서 `FINISHED` 상태이며 설정, OOF, 시드별 OOF, 시험 예측, 제출 파일과 필요한 초기 점수 근거 산출물을 모두 열어 확인했다.
메인 MLflow 산출물 위치는 `/Users/taemyungheo/workspace/github/kaggle/predicting-smartphone-addiction/mlruns/1/<run_id>/artifacts`다.

## 기준 재현

새 기준 난수 42 OOF AUC는 `0.9679475905933742`다.
기록된 기준 3시드 실행의 난수 42 OOF와 AUC 차이는 `0.0`, 스피어만은 `1.0`, 최대 절대 예측 차이는 `0.0`이다.
기록된 기준 난수 42 OOF SHA-256은 `3aeff53e9d13b68297f6d53934f280d8fe6a0b3f02a8f24efb2a8c466e2e71b5`다.

## 난수 42 짝비교

후보 난수 42 OOF AUC는 `0.9684155449710514`이고 기준 대비 차이는 `+0.0004679543776772`다.
후보는 바깥쪽 5분할 모두에서 기준을 이겼다.

| 분할 | 기준 AUC | 후보 AUC | 차이 |
|---:|---:|---:|---:|
| 0 | 0.9673017424 | 0.9677731400 | +0.0004713976 |
| 1 | 0.9680726869 | 0.9684294161 | +0.0003567292 |
| 2 | 0.9679679702 | 0.9685121406 | +0.0005441704 |
| 3 | 0.9685762325 | 0.9690167729 | +0.0004405404 |
| 4 | 0.9678255070 | 0.9683480793 | +0.0005225723 |

## 확정 실행

후보 3시드 평균 OOF AUC는 `0.9685106348128161`다.
난수별 OOF AUC는 난수 42 `0.9684155449710514`, 난수 43 `0.9683928509048039`, 난수 44 `0.9683989413849938`이다.
초기 점수 근거에는 3시드와 5분할의 15개 좌표가 모두 있으며 SHA-256은 `fb385089da8bc64e10b0e29f90912ff7b24f91caad1ea36cc199e76bb825a0d6`이다.
초기 로짓의 학습 최솟값은 분할과 시드에 따라 약 `-10.04`부터 `-12.76`까지였고 최댓값은 고정 clip 경계의 로짓 `13.815509557935018`이었다.

## 진입과 중복 진단

후보는 진입 하한 `0.9593676105620947`을 통과했다.
최근접 구성원은 `exp209_issue505_lgb_lr_onehot_init`이고 시드 평균 스피어만은 `0.9941472664108334`다.
스피어만이 중복 문턱 `0.998` 미만이므로 원자 교체가 아니라 일반 추가 경로로 판정했다.

## 후보 풀 판정

`issue521-exp210-admission-v2`는 `candidate-pool-v2` 핵심 결합 방식 세 개에서 현재 풀과 후보 포함 풀을 비교했다.
전체 nested OOF 차이는 `-0.0000041446702229`이고 후보 포함 풀의 분할 승리는 `1/5`다.
분할별 차이는 `[-0.0000036995, -0.0000021323, +0.0000012480, -0.0000052018, -0.0000109380]`이다.
양의 기여 불변식을 통과하지 못했으므로 후보를 등록하지 않았다.

## 기록과 진단

- 화면 판정 기록: `artifacts/issue521-exp210-screen.json`
- 확정 준비 기록: `artifacts/issue521-exp210-confirm.json`
- 최종 판정 기록: `artifacts/issue521-exp210-final.json`
- 후보 풀 판정 기록: `artifacts/judgments/issue521-exp210-admission-v2.yaml`
- 최종 판정 기록 SHA-256: `8faa717ed556d78f72fb3e58d0ea9c47c6683b7d8fd6065d175c37a6b45728c7`

단일 시드 재사용을 시도한 실행 `3d54b0424f9d481bb27406be9504d25f`는 `training_row_evidence.json`을 무조건 요구하는 현재 재사용 경로 때문에 학습 전에 실패했다.
초기 점수 근거도 재사용 경로가 복원하지 않으므로 우회하지 않고 3시드를 모두 새로 실행했다.
첫 후보 풀 판정 기록 `issue521-exp210-admission`은 작업 디렉터리의 `data/` 심볼릭 링크가 저장소 밖을 가리킨다는 입력 경계 검사로 판정 불가가 됐다.
입력 내용을 바꾸지 않고 저장소 내부 실제 파일로 바꾼 뒤 `issue521-exp210-admission-v2`에서 정상 판정했다.
