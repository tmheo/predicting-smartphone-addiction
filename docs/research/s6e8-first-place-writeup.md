# S6E8 1등 해법 원문 조사

이 문서는 GitHub 이슈 [1등 해법의 구성과 근거를 원문에서 확정한다](https://github.com/tmheo/predicting-smartphone-addiction/issues/545)의 조사 결과를 기록한다.
조사 대상은 Chris Deotte의 [1등 write-up](https://www.kaggle.com/competitions/playground-series-s6e8/writeups/1st-place-distributed-intelligence-nvidia-infe), 그 글에 붙은 댓글, 글에 삽입된 작성자 소유 그림, 작성자가 이 대회에 공개한 Kaggle 노트북이다.
본문 조사 시점은 2026-09-01이며, 최종 순위표와 댓글은 Kaggle의 1차 자료 전용 API로 다시 확인했다.
댓글은 2026-09-03에 Chrome으로 다시 확인했고, Kaggle이 세는 47개 가운데 40개가 표시됐으며 나머지 7개는 삭제 또는 숨김으로 보인다.
2026-09-03 재확인에서 늘어난 작성자 답변은 `## 댓글에서 추가로 확인한 것`에 모았고, 그 답변으로 바뀐 사실은 요약과 확정 범위 표에도 반영했다.

## 결론 요약

원문으로 확정할 수 있는 우승 해법의 중심은 특정한 하나의 비법 특성이 아니라, 약 1주 반 동안 언어 모형 여러 개가 독립적으로 많은 표 자료 모형을 만들고 서로의 발견을 공유한 뒤 수백 개 모형을 로지스틱 회귀로 쌓은 대규모 결합이다.
그 과정에서 가장 강한 단일 모형은 RealMLP였고, 정확한 그림 표기 기준 바깥쪽 예측 AUC는 `0.970706453`, 공개 순위표 점수는 `0.97174`였다.
가장 강한 XGBoost는 바깥쪽 예측 AUC `0.970203373`, 공개 순위표 점수 `0.97030`이었다.
최종 결합의 공개 순위표 점수는 제출 화면에서 `0.97207`, 진행 그림의 반올림 표기에서 `0.97206`이며, 공식 최종 순위표 점수는 `0.97176`이다.
공개 점수 기준 결합이 최고 RealMLP보다 얻은 이득은 `0.00033`이다.

2026-09-03에 다시 읽은 댓글에서 작성자는 단일 모형과 최종 제출의 핵심 수치를 직접 밝혔다.
최고 RealMLP는 PyTabKit의 기본 `RealMLP_TD_Classifier`이고 구조 변경은 없었으며, 원시 특성만으로 CV `0.9660`, 이전 대회와 비슷한 기본 특성 생성으로 CV `0.9695`, 튜닝으로 `+6e-4`, 고급 특성으로 다시 `+6e-4`를 얻었다.
최종 두 제출은 456개 모형을 NVIDIA cuML 로지스틱 회귀로 쌓은 결합(CV `0.97098`, Public LB `0.97207`, Private LB `0.97176`)과 GPT 5.6이 만든 최고 RealMLP 단일 모형(CV `0.97070`, Public LB `0.97174`, Private LB `0.97145`)이었다.
단일 RealMLP의 Private LB `0.97145`는 공식 최종 순위표 2위 `0.97123`보다 높으므로, 본문의 `단일 모형이 1위`라는 주장은 비공개 점수에서도 성립한다.

그러나 최종 특성 목록, RealMLP 설정값, XGBoost 설정값, 검증 분할과 시드, 456개 구성원의 목록, 결합기의 입력 변환과 규제 강도와 가중치, 학습 장비와 총 비용은 여전히 공개되지 않았다.
작성자는 구체적인 특성 아이디어를 다음 Playground 대회에서 먼저 시험한 뒤 공개하겠다고 댓글에서 밝혔다.
따라서 이 글은 우승 과정의 보고서로는 유용하지만 최종 해법을 다시 실행할 수 있는 기술 명세는 아니다.
작성자의 공개 입문 노트북 네 개는 초기 가설과 작업 습관을 보여 주지만, 공개 점수가 훨씬 낮고 원문도 이를 최종 모형 코드라고 연결하지 않으므로 최종 해법의 대리물로 취급하면 안 된다.

가장 중요한 해석 경계는 제목의 `NVIDIA Inference Hub`다.
원문에서 NVIDIA 서비스는 표 자료 모형이 시험 행을 예측하는 추론 장비가 아니라, Fable 5가 여러 언어 모형을 평가하고 실험 과제를 맡기는 데 사용한 언어 모형 제공 통로다.
최종 RealMLP나 XGBoost를 NVIDIA 추론 서비스에 배포했다는 근거는 없다.

## 근거 수준

이 문서는 근거를 다음 세 단계로 구분한다.

| 수준 | 뜻 |
| --- | --- |
| A | 우승 글 본문, 삽입 그림, 작성자가 댓글에 직접 쓴 답변, Kaggle 최종 순위표가 직접 보여 주는 사실 |
| B | 작성자가 공개한 같은 대회의 노트북과 그 실행 산출물이 보여 주는 보조 사실이지만 최종 해법 적용 여부는 확인되지 않음 |
| C | A와 B를 함께 읽어 얻은 분석상 추론이며 원문이 직접 말한 사실은 아님 |

작성자의 가치 판단과 과장은 별도로 `작성자 해석`이라고 표시한다.
다른 참가자가 댓글에 쓴 수치와 해석은 우승 해법의 근거가 아니므로 `제3자 댓글`로 따로 표시하고 근거 수준을 매기지 않는다.

## 축별 확정 범위

| 축 | 확정 가능한 내용 | 공개되지 않은 내용 | 근거 수준 |
| --- | --- | --- | --- |
| 자료 활용 | 첫 자율 에이전트가 대회 설명을 읽고 대회 자료를 내려받았으며, 공개 입문 노트북은 대회 자료만 자료 원천으로 선언한다. | 최종 해법의 원본 자료, 외부 자료, 의사 라벨 사용 여부는 밝히지 않았다. | A, B |
| 특성 | 공개 입문 노트북에는 화면 시간 구성, 잔차, 비율, 수면 대비 화면 시간, 알림과 앱 실행 강도, 결측 개수 같은 14개 파생 특성이 있다. 작성자 댓글에 따르면 RealMLP는 이전 대회와 비슷한 기본 특성 생성으로 CV `0.9660`에서 `0.9695`로 올랐고 고급 특성으로 `+6e-4`를 더 얻었으며, XGBoost 특성은 이전 Playground 특성 생성의 발전판이고, 결측값 연구에서 새 신호 원천을 찾았다. | ChatGPT Pro가 찾았다는 새 특성, 고급 특성의 정의, 결측 신호의 형태, 최종 RealMLP 및 XGBoost 특성 목록은 공개하지 않았다. 작성자는 다음 Playground에서 먼저 시험한 뒤 공개하겠다고 했다. | A, B |
| 모형 | 최고 단일 모형 계열은 RealMLP와 XGBoost이고, 최고 RealMLP는 PyTabKit 기본 `RealMLP_TD_Classifier`이며 구조 변경이 없다. 최종 결합 규모는 진행 그림 기준 449개, 작성자 댓글 기준 456개 모형이다. | RealMLP 튜닝 설정값, XGBoost 설정, 나머지 모형 계열과 각 계열 수량, 449와 456의 차이 원인은 공개하지 않았다. | A |
| 검증 | 최고 RealMLP의 CV는 원시 특성 `0.9660`, 기본 특성 `0.9695`, 튜닝 `+6e-4`, 고급 특성 `+6e-4`로 분해되고, 최고 XGBoost의 바깥쪽 예측 AUC와 최종 결합의 CV `0.97098`도 공개됐다. | 최종 해법의 분할 수, 분할표, 시드, 전처리 적합 범위, 반복 횟수, 결합 CV가 중첩 검증인지 여부는 공개하지 않았다. | A |
| 결합 | 10개에서 50개 모형 단위로 새 모형을 추가하면서 검증 점수와 공개 점수를 확인했고, 최종 결합은 456개 모형을 NVIDIA cuML 로지스틱 회귀로 쌓은 것이다. | 결합기의 입력 변환, 규제 강도, 구성원 선택 기준, 가중치, 결합기 검증 방식은 공개하지 않았다. | A |
| 예측 생성 | 다섯 분할 평균 예측은 공개 입문 노트북에서 확인되지만, 최종 제출의 예측 생성 절차는 없다. | 최종 재학습 여부, 분할 평균 방식, 자료 변환, 배치 크기, 장비별 실행 절차는 공개하지 않았다. | B |
| 계산 자원 | 1단계 4일, 전체 약 1주 반, 여러 언어 모형과 반복적인 Kaggle 제출을 사용했다. 작성자 댓글에 따르면 에이전트는 4시간에서 12시간 단위로 자율 실행했고, 며칠에 걸쳐 수만 번의 실험을 했으며, 결합기는 GPU 라이브러리인 cuML로 학습했다. | 표 자료 모형의 CPU와 GPU 종류, 동시 실행 수, 총 학습 시간, 저장 공간, API 호출량과 비용은 공개하지 않았다. | A |
| 재현 가능성 | 공개 입문 노트북 네 개와 그 산출물은 다시 내려받을 수 있다. 작성자 댓글은 50개가 넘는 `discovery.md` 발견 기록 파일과 1M 토큰 이상의 문맥을 압축 없이 쓰는 기억 방식을 설명한다. | 최종 코드, 고정 환경, 특성, 모형 파일, 바깥쪽 예측, 시험 예측, 결합 명세, 지시문, 발견 기록 파일과 공유 지식 저장소는 공개하지 않았다. | A, B |

## 단계별 구성

### 1단계: 단일 자율 에이전트

확정 사실 A: 작성자는 대회 종료 약 1주 반 전에 참가했고, Codex GPT 5.6 Sol 자율 에이전트 하나에 대회 설명 확인, 자료 내려받기, 모형 생성, 검증, 제출을 맡겼다고 썼다.
확정 사실 A: 에이전트는 10개에서 50개 모형씩 묶음을 추가할 때마다 새 결합 검증 점수를 계산하고 Kaggle에 제출했다.
확정 사실 A: 4일 뒤 결합은 380개 모형에 이르렀고 공개 순위표 약 10위권에 들었다고 원문이 보고한다.
이 근거는 [우승 글의 1단계 설명](https://www.kaggle.com/competitions/playground-series-s6e8/writeups/1st-place-distributed-intelligence-nvidia-infe)과 [모형 수 및 공개 점수 진행 그림](https://github.com/cdeotte/Kaggle_Images/blob/2aebfa0d0ce386412d20ff885e76cacb8cf04ac1/Aug-2026/phases.png)이다.

분석 판정 C: `380개 모형`은 서로 다른 380개 모형 파일이나 분할 모형을 뜻할 수 있지만, 원문이 단위를 정의하지 않아 독립적인 모형 설정 380개라고 확정할 수 없다.
분석 판정 C: 공개 순위표 제출을 각 모형 묶음 뒤에 반복했으므로 공개 순위표에 대한 적응이 있었을 가능성이 높지만, 제출 선택 규칙과 최종 비공개 점수 선택 절차가 없어서 그 영향은 정량화할 수 없다.

### 2단계: 두 에이전트의 경쟁과 지식 공유

확정 사실 A: Fable 5와 두 번째 GPT 5.6 Sol이 합류했고, 두 에이전트는 이전 작업과 탐색 분석을 읽은 뒤 신경망과 XGBoost 단일 모형 성능을 두고 경쟁했다.
확정 사실 A: 대부분 독립적으로 작업하되 뒤처진 에이전트에게 앞선 에이전트의 방법을 공유하는 방식이었고, 그림에는 두 에이전트가 공유 지식 저장소를 함께 쓰는 구조가 표시된다.
확정 사실 A: [에이전트 구조 그림](https://github.com/cdeotte/Kaggle_Images/blob/2aebfa0d0ce386412d20ff885e76cacb8cf04ac1/Aug-2026/agents.png)은 GPT 5.6 Sol, Fable 5, ChatGPT Pro 여러 작업, NVIDIA 서비스의 여섯 언어 모형이 공유 지식으로 결과를 돌려주는 흐름을 보여 준다.

| 경쟁 축 | 만든 주체 | 그림의 최종 이름 | 바깥쪽 예측 AUC | 공개 순위표 점수 |
| --- | --- | --- | ---: | ---: |
| RealMLP | GPT 5.6 Sol | `RealMLP repair20` | `0.970706453` | `0.97174` |
| RealMLP | Fable 5 | `RealMLP v4x` | `0.970566543` | 미공개 |
| XGBoost | Fable 5 | `XGB v5` | `0.970203373` | `0.97030` |
| XGBoost | GPT 5.6 Sol | `XGB Explore24` | `0.969973091` | 미공개 |

정확한 바깥쪽 예측 수치와 모형 이름은 [단일 모형 진행 그림](https://github.com/cdeotte/Kaggle_Images/blob/2aebfa0d0ce386412d20ff885e76cacb8cf04ac1/Aug-2026/top1s.png)에 직접 표시돼 있다.
본문의 `RealMLP CV 0.97070 LB 0.97174`와 `XGB CV 0.97020 LB 0.97030`은 이 그림 수치를 반올림한 값이다.
확정 사실 A: 작성자는 댓글에서 최고 RealMLP가 GPT 5.6의 모형이며 Private LB `0.97145`를 기록했다고 밝혔고, Fable 5가 한동안 단일 모형 경쟁에서 앞서다가 8월 28일 무렵 Fable 5의 발견을 GPT 5.6에 공유한 뒤 GPT 5.6이 최고 RealMLP를 만들었다고 설명했다.
확정 사실 A: 작성자는 Fable 5가 창의적인 돌파구를 더 많이 만들었고 Codex GPT 5.6은 신중한 코딩과 특성 생성 탐정 작업에 강했다고 두 에이전트의 성향을 구분했다.

분석 판정 C: 최고 RealMLP의 공개 점수는 바깥쪽 예측 AUC보다 `0.001033547` 높고, 최고 XGBoost의 차이는 반올림 본문 기준 약 `0.00010`이다.
이 차이는 시험 예측이 여러 분할 모형의 평균일 가능성, 공개 자료 분포 차이, 순위표 잡음 등 여러 원인이 있을 수 있으므로 원문만으로 한 가지 원인에 귀속할 수 없다.

### 3단계: ChatGPT Pro 병렬 조사

확정 사실 A: Fable 5가 코드와 자료를 `tar.gz` 묶음으로 만들고 지시문을 작성했으며, 사람이 이를 ChatGPT Pro에 수동으로 올렸다.
확정 사실 A: 여러 묶음과 지시문을 병렬로 제출했고, 각 작업은 약 2시간 뒤 발견 내용을 ZIP 파일로 돌려줬다.
확정 사실 A: 원문은 그 발견을 GPT 5.6 Sol과 Fable 5가 반영한 뒤 최고 단일 모형의 검증 점수와 공개 점수가 개선됐다고 보고한다.
근거는 [우승 글의 3단계 설명](https://www.kaggle.com/competitions/playground-series-s6e8/writeups/1st-place-distributed-intelligence-nvidia-infe)이다.

작성자 해석: 작성자는 ChatGPT Pro가 자신의 8년 Kaggle 경험에서도 보지 못한 새 표 자료 특성 공학을 발견했고 다른 대회에도 적용될 수 있다고 평가한다.
분석 판정 C: 그 특성의 이름, 수식, 제거 실험, 개선 폭이 공개되지 않았으므로 새로움, 범용성, 실제 기여도는 검증할 수 없다.

### 4단계: NVIDIA 서비스의 여러 언어 모형

확정 사실 A: Fable 5는 [NVIDIA Build](https://build.nvidia.com/)의 언어 모형 후보를 평가하고 `Nemotron 3 Ultra`, `DeepSeek V4 Pro`, `Kimi K3`, `Gemini 3.7 Flash`, `Opus 5`, `Qwen 3.8 27B`에 결합 검증 점수와 공개 점수를 높이는 과제를 여러 개씩 맡겼다.
확정 사실 A: 원문은 NVIDIA 서비스에 약 150개 언어 모형을 고를 수 있었다고 작성자가 보고한다.
근거는 [우승 글의 4단계 설명](https://www.kaggle.com/competitions/playground-series-s6e8/writeups/1st-place-distributed-intelligence-nvidia-infe)과 [에이전트 구조 그림](https://github.com/cdeotte/Kaggle_Images/blob/2aebfa0d0ce386412d20ff885e76cacb8cf04ac1/Aug-2026/agents.png)이다.

분석 판정 C: 이 단계에서 NVIDIA가 제공한 것은 문제 해결에 참여한 언어 모형 호출 환경이다.
분석 판정 C: 표 자료 분류 모형의 학습이나 시험 자료 예측을 NVIDIA 추론 서비스에서 실행했다는 서술은 없다.
분석 판정 C: 제목의 `NVIDIA Inference Hub`를 최종 RealMLP 또는 449개 결합의 실행 구조로 해석하면 원문보다 넓은 주장이다.

## 점수와 순위의 정확한 해석

### 공개 순위표

확정 사실 A: [최종 제출 화면 그림](https://github.com/cdeotte/Kaggle_Images/blob/2aebfa0d0ce386412d20ff885e76cacb8cf04ac1/Aug-2026/single_model.png)은 결합 제출 `0.97207`, 단일 모형 제출 `0.97174`, 당시 2위 `0.97154`를 표시한다.
따라서 이 화면에서는 단일 RealMLP도 2위보다 `0.00020` 높고, 결합은 단일 RealMLP보다 `0.00033` 높다.
진행 그림은 마지막 결합을 `449 models`, `Best v70: 0.97206`으로 표기하며, 제출 화면과의 `0.00001` 차이는 표시 자릿수와 그림 작성 과정의 반올림 차이로 읽는 것이 타당하다.

### 공식 최종 순위표

확정 사실 A: Kaggle 최종 순위표 API는 Chris Deotte 팀을 1위, 최종 점수 `0.97176`으로 반환하며 이는 [공식 최종 순위표](https://www.kaggle.com/competitions/playground-series-s6e8/leaderboard)에서 확인할 수 있다.
공개 제출 화면의 결합 `0.97207`과 최종 점수 `0.97176`의 차이는 `-0.00031`이다.

확정 사실 A: 작성자는 2026-09-03 기준 댓글에서 최종 두 제출의 점수를 직접 밝혔다.
결합 제출은 CV `0.97098`, Public LB `0.97207`, Private LB `0.97176`이고, 단일 RealMLP 제출은 CV `0.97070`, Public LB `0.97174`, Private LB `0.97145`다.
따라서 결합이 단일 RealMLP보다 얻은 이득은 CV `+0.00028`, Public LB `+0.00033`, Private LB `+0.00031`로 세 눈금에서 비슷하다.

확정 사실 A: 2026-09-03에 공식 Kaggle 명령줄 도구로 다시 조회한 최종 순위표에서 2위 MILANFX의 점수는 `0.97123`이다.
단일 RealMLP의 Private LB `0.97145`는 이 값보다 `0.00022` 높으므로, 본문의 `단일 모형이 혼자서도 1위`라는 주장은 공개 순위표와 비공개 순위표 모두에서 성립한다.
이 문서의 2026-09-01 판은 단일 RealMLP의 비공개 점수가 없어 이 주장을 미확정으로 두었으나, 작성자 댓글로 확정됐다.

## 자료와 특성

### 최종 해법에서 확정되는 것

확정 사실 A: 1단계 에이전트가 대회 설명을 읽고 대회 자료를 내려받았다는 것만 본문에 명시돼 있다.
분석 판정 C: 본문에는 원본 생성 자료, 별도 외부 자료, 의사 라벨, 공개 예측 배열을 썼다는 언급도 없고 쓰지 않았다는 선언도 없다.
따라서 최종 해법의 자료 범위는 `대회 자료 사용 확인, 추가 자료 여부 미확정`으로 기록해야 한다.

확정 사실 A: 원문은 ChatGPT Pro가 새 특성 아이디어를 찾았다고만 말하며 실제 수식은 공개하지 않는다.
확정 사실 A: Ahmed Malik이 XGBoost를 약 `0.968`에서 `0.970`으로 올린 특성을 묻자, 작성자는 이전 Playground 대회에서 쓰던 특성 생성의 발전판이라고 답했고 구체적인 내용은 다음 Playground에서 먼저 시험한 뒤 공개하겠다고 했다.
확정 사실 A: 작성자는 에이전트가 정체기에 들어섰을 때 결측값을 연구해 그에 맞는 특성을 찾았는지 물었고, 그 뒤 에이전트가 결측을 여러 시간 연구해 새 신호 원천을 찾아 모형을 개선했다고 밝혔다.
자세한 문답은 `## 댓글에서 추가로 확인한 것`에 있다.

### 공개 입문 노트북에서만 확인되는 것

작성자는 대회 초기에 [기초 탐색 분석](https://www.kaggle.com/code/cdeotte/basic-eda-smartphone-addiction), [XGBoost 입문](https://www.kaggle.com/code/cdeotte/simple-xgb-starter), [CatBoost 입문](https://www.kaggle.com/code/cdeotte/simple-cat-starter), [신경망 입문](https://www.kaggle.com/code/cdeotte/simple-nn-starter)을 공개했다.
이 네 노트북은 대회 학습 자료 `691,369`행, 시험 자료 `296,302`행, 원시 예측 변수 12개를 사용하고 대회 자료 외 별도 자료 원천을 선언하지 않는다.
탐색 노트북은 모든 원시 예측 변수에 결측이 있고 화면 시간 관련 변수에 가장 큰 한 변수 신호가 있다고 요약한다.

세 입문 모형 노트북이 공통으로 만든 파생 특성은 다음 14개다.

- 행별 결측 개수
- 소셜 미디어, 게임, 업무와 학습 화면 시간의 합
- 일일 화면 시간에서 세 구성 시간을 뺀 시간 회계 잔차
- 주말 화면 시간에서 일일 화면 시간을 뺀 차이
- 주말 화면 시간과 일일 화면 시간의 비율
- 일일 화면 시간 중 소셜 미디어 비중
- 일일 화면 시간 중 게임 비중
- 일일 화면 시간 중 업무와 학습 비중
- 수면 시간에서 일일 화면 시간을 뺀 차이
- 일일 화면 시간과 수면 시간의 비율
- 화면 시간당 앱 실행 횟수
- 화면 시간당 알림 수
- 앱 실행당 알림 수
- 알림 수와 앱 실행 횟수의 합

이 파생을 더하면 원시 예측 변수 12개에서 총 26개 예측 변수로 늘어난다.
XGBoost는 세 범주 변수를 원-핫 부호화하고 수치 결측은 그대로 처리하며, CatBoost는 세 범주 변수를 본래 범주로 처리하고, 공개 신경망은 수치 변수의 분할별 중앙값 대체 및 표준화와 범주 임베딩을 사용한다.

입문 노트북 실행 산출물은 모두 5겹 층화 분할과 시드 42를 쓰며 다음 결과를 낸다.

| 공개 입문 노트북 | 전체 바깥쪽 예측 AUC | 다섯 분할 실행 시간 | 최종 해법 최고 단일 모형과의 차이 |
| --- | ---: | ---: | ---: |
| XGBoost | `0.964812` | `23.77분` | `-0.005894` |
| CatBoost | `0.962906` | `80.81분` | `-0.007800` |
| 단순 PyTorch MLP | `0.939737` | `19.11분` | `-0.030969` |

분석 판정 C: 최고 단일 RealMLP의 `0.970706453`과 공개 XGBoost 입문의 `0.964812` 사이에도 약 `0.005894`가 남는다.
따라서 공개된 14개 파생 특성과 입문 설정만으로 우승 단일 모형의 성능을 설명할 수 없다.
분석 판정 C: 이 네 노트북은 1단계와 2단계의 출발점이나 공유 지식 일부였을 수 있지만, 원문이 연결 관계를 명시하지 않으므로 적용 여부는 추측으로 남긴다.

## 검증과 결합

확정 사실 A: 최종 RealMLP와 XGBoost 그림은 `OOF AUC`라고 명시한 바깥쪽 예측 점수를 시간순으로 비교한다.
확정 사실 A: 1단계 에이전트는 새 모형 묶음을 추가할 때마다 결합 검증 점수를 다시 계산했다.
확정 사실 A: 마지막 결합 진행 그림에는 449개 모형이 표시된다.

공개되지 않은 검증 항목은 다음과 같다.

- 분할 개수와 실제 분할 행
- 층화 여부와 분할 시드
- 모든 전처리의 분할 안쪽 적합 여부
- RealMLP 내부 앙상블과 초기화 수
- 여러 실험을 같은 바깥쪽 예측에 반복 선택하면서 생긴 선택 편향
- 결합 구성원을 고른 기준과 중복 처리
- 결합 CV `0.97098`이 중첩 검증인지, 결합기 적합에 쓴 바깥쪽 예측을 그대로 채점한 값인지 여부
- 공개 순위표를 실험 선택에 반영한 정확한 규칙

확정 사실 B: 공개 입문 노트북은 5겹 층화 분할, 시드 42, 분할별 시험 예측 평균, 바깥쪽 예측 저장을 사용한다.
분석 판정 C: 이 규율이 최종 작업에도 이어졌을 가능성은 있지만, 최종 RealMLP 그림과 우승 글에는 그 계약이 없으므로 최종 해법의 검증 명세로 승격할 수 없다.

확정 사실 A: `449 models`와 `Stack Progress`라는 그림 제목은 대규모 결합이 최종 점수의 기반임을 확인한다.
확정 사실 A: 작성자는 댓글에서 최종 결합이 `456 models combined with NVIDIA cuML logistic regression`이며 CV `0.97098`이라고 밝혔다.
따라서 결합은 2단계 학습기로 로지스틱 회귀를 쓰는 쌓기이며, 이 문서의 2026-09-01 판이 미확정으로 두었던 결합 방식은 확정됐다.
분석 판정 C: 그림의 449개와 댓글의 456개 차이는 원문이 설명하지 않으며, 그림 작성 뒤 구성원이 더해졌을 가능성이 있지만 확인할 수 없다.
분석 판정 C: cuML은 GPU 라이브러리이므로 결합기 학습은 GPU에서 이뤄졌다고 읽는 것이 타당하지만, 입력 변환, 규제 강도, 가중치의 부호 제약, 구성원 선택 규칙, 결합기 자체의 검증 방식은 여전히 공개되지 않았다.

## 예측 생성과 계산 자원

최종 해법의 예측 생성 절차는 공개되지 않았다.
최종 모형을 전체 학습 자료로 다시 맞췄는지, 여러 분할 모형의 시험 예측을 평균했는지, 449개 결과를 어떤 정밀도와 순서로 결합했는지 원문으로 알 수 없다.

공개 입문 노트북의 Kaggle 메타데이터는 XGBoost, CatBoost, 단순 신경망에 NVIDIA Tesla T4 실행 환경이 연결됐음을 보여 준다.
다만 XGBoost와 CatBoost 입문 코드의 기본 설정은 CPU이고, 단순 신경망은 CUDA가 있으면 GPU를 쓴다.
이 환경 정보는 입문 노트북에만 해당하며 우승 RealMLP, XGBoost, 449개 결합의 학습 장비를 증명하지 않는다.

원문으로 확인되는 계산 규모의 하한은 다음과 같다.

- 단일 자율 에이전트가 4일 동안 380개 모형 규모까지 확장했다.
- 전체 작업 기간은 약 1주 반이었다.
- 2단계에는 GPT 5.6 Sol 둘과 Fable 5가 참여했다.
- 3단계에는 여러 개의 2시간 ChatGPT Pro 작업이 병렬로 수행됐다.
- 4단계에는 NVIDIA 서비스를 통해 여섯 종류의 언어 모형이 여러 과제를 맡았다.
- 마지막 진행 그림에는 449개 모형과 약 70번째 결합 판본으로 보이는 `v70` 표기가 있다.

분석 판정 C: `v70`을 정확히 70번의 독립 결합 탐색이라고 해석할 근거는 없으며, 작성자의 내부 판본 이름일 뿐이다.
분석 판정 C: 언어 모형 호출 규모는 컸지만 표 자료 모형 학습 장비와 비용이 빠져 있어 총 계산량을 GPU 시간이나 비용으로 환산할 수 없다.

## 재현 가능성 판정

최종 해법의 재현 가능성은 `낮음`이다.
순위와 주요 점수, 작업 단계, 단일 모형 계열, 마지막 결합 크기는 확인되지만 같은 결과를 만드는 데 필요한 실행 명세가 없다.

최소 재현 묶음에서 빠진 항목은 다음과 같다.

- 정확한 학습 및 시험 자료 해시와 추가 자료 장부
- 최종 특성 생성 코드와 특성 목록
- RealMLP 및 XGBoost 설정값과 라이브러리 판본
- 분할표, 시드, 초기화, 반복 횟수
- 449개 구성원의 식별자와 바깥쪽 예측 및 시험 예측
- 구성원 선택 기준, 결합식, 가중치
- 최종 제출 파일과 중간 산출물 해시
- 언어 모형 지시문, 응답 묶음, 공유 지식 저장소
- 실행 장비, 동시 실행 방식, 총 시간과 비용

작성자가 공개한 네 입문 노트북은 Apache License 2.0 적용 대상인 공개 Kaggle 노트북이며, 본 조사에서는 코드를 복사하거나 저장소 산출물로 재배포하지 않고 구조와 실행 결과만 검토했다.
각 공개 판본은 아래 출처 목록에 판본 번호와 내려받은 소스 해시를 기록했다.

## 작성자 해석과 검증 가능한 사실의 분리

| 작성자 해석 | 원문에서 검증되는 범위 | 판정 |
| --- | --- | --- |
| 최전선 언어 모형은 데이터 과학 Grandmaster 수준이다. | 여러 언어 모형이 만든 실험 중 RealMLP와 XGBoost의 강한 점수, 최종 1위는 확인된다. | 가치 판단이며 통제된 사람 대비 실험은 없다. |
| 단일 모형이 1위를 한다. | 단일 RealMLP가 공개 화면에서 당시 2위보다 `0.00020` 높고, 댓글에서 밝힌 Private LB `0.97145`가 공식 2위 `0.97123`보다 `0.00022` 높다. | 공개 순위표와 비공개 순위표 모두에서 참이다. |
| ChatGPT Pro가 새롭고 모든 표 자료 대회에 유용한 특성을 찾았다. | 반영 뒤 점수가 개선됐다는 작성자 보고와, 고급 특성 전체가 RealMLP CV에 `+6e-4`를 더했다는 댓글 수치가 있다. | 특성 정의와 제거 실험이 없고 `+6e-4`가 ChatGPT Pro 몫만인지도 알 수 없어 독립 검증 불가다. |
| 에이전트가 단일 모형을 사람보다 잘 개선했다. | 에이전트가 단일 모형의 오류를 연구해 필요한 특성과 조정을 더했고, 초기 단일 모형이 380개 결합을 이겼다는 작성자 보고가 있다. | 작성자의 관찰이며 사람 기준선과 맞춘 비교는 없다. |
| NVIDIA Inference Hub가 우승에 기여했다. | 그 서비스에서 고른 여섯 언어 모형이 실험 과제를 맡은 사실은 확인된다. | 표 자료 모형 추론 기반 시설이라는 뜻은 아니다. |
| 에이전트의 작업은 사람이 할 수 있는 수준을 넘었다. | 1주 반 동안 449개 모형과 1위 결과를 만든 작업량은 확인된다. | 인간 기준선과 비용을 맞춘 비교가 없어 정량 주장은 아니다. |

## 댓글에서 추가로 확인한 것

### 2026-09-01 상태

본문 조사 시점의 연결 토론에는 댓글 5개가 있었고 작성자의 답변은 없었다.
한 댓글은 공개 점수 기준 최고 단일 모형에서 결합까지의 이득 `0.00033`을 지적했고, 이는 삽입 그림의 `0.97207 - 0.97174`와 일치한다.
다른 댓글은 XGBoost를 약 `0.968`에서 `0.970`으로 올린 특성을 물었지만 당시에는 답이 없었다.
또 다른 댓글은 단일 GPU에서의 메모리 절충을 물었지만, 원문이 실제로 공개한 것은 표 자료 모형의 분산 추론 구조가 아니므로 질문의 전제가 원문보다 넓다.

### 2026-09-03 갱신

2026-09-03에 [우승 글 페이지](https://www.kaggle.com/competitions/playground-series-s6e8/writeups/1st-place-distributed-intelligence-nvidia-infe)를 Chrome으로 다시 읽었다.
Kaggle은 댓글 수를 47개로 표시했지만 실제로 보인 것은 40개였고, 나머지 7개는 삭제 또는 숨김으로 보인다.
보이지 않는 댓글 가운데 결측에 관한 작성자 답변이 있었다는 사실은 Georgy Mamarin의 댓글이 `Your missingness answer to Exposed`라고 언급해 간접적으로만 알 수 있으며, 그 답변 원문은 확인하지 못했다.
아래는 작성자 Chris Deotte의 답변 전부를 질문자별로 정리한 것이며, 작성자 답변은 모두 근거 수준 A다.

#### 단일 모형 점수 분해: Vladimir Demidov의 질문에 대한 답변

Vladimir Demidov는 RealMLP가 PyTabKit 분류기인지 직접 만든 모형인지, 그리고 고급 특성과 모형 개선 및 튜닝의 개선 비율이 어느 정도인지 물었다.
작성자는 `We used the stock PyTabKit and RealMLP_TD_Classifier, no custom architecture`라고 답해 구조 변경이 없음을 확정했다.
점수 분해는 원시 특성으로 `CV 0.9660`, 이전 대회와 비슷한 기본 특성 생성으로 `CV 0.9695`, 튜닝으로 `6e-4`, 고급 특성으로 `6e-4`였다.
이 네 단계를 더하면 `0.9707`이 되어 본문의 `CV 0.97070`과 맞는다.

분석 판정 C: 원시 특성에서 최종까지 `+0.0047` 가운데 기본 특성 생성이 `+0.0035`로 가장 크고, 튜닝과 고급 특성이 각각 `+0.0006`으로 같다.
분석 판정 C: 이 분해는 RealMLP 하나에 대한 것이며 XGBoost의 분해는 공개되지 않았다.

#### 최종 두 제출: thisray의 질문에 대한 답변

thisray는 `0.97174`가 공개 점수인지와 최종 비공개 점수를 물었다.
작성자는 최종 두 제출이 `456 models combined with NVIDIA cuML logistic regression`으로 만든 쌓기와 최고 단일 모형이라고 답했다.
쌓기는 `CV 0.97098, Public LB 0.97207, Private LB 0.97176`, 단일 모형은 `GPT 5.6's RealMLP with CV 0.97070, Public LB 0.97174, Private LB 0.97145`였다.

같은 답변에서 작성자는 두 에이전트의 성향을 `Fable seemed to make more creative break throughs while Codex did more careful coding (and good feature engineering detective work)`라고 구분했다.
작성자가 `GPT vs Fable plots`라고 부른 그림에서 Fable이 한동안 최고 단일 모형을 쥐고 있었고, 8월 28일 무렵 Fable의 발견을 GPT에 많이 공유한 뒤 GPT가 이를 최고 RealMLP에 밀어 넣었다고 설명했다.
분석 판정 C: 이 문서가 확인한 삽입 그림 가운데 두 에이전트의 모형 이름을 함께 표시하는 [단일 모형 진행 그림](https://github.com/cdeotte/Kaggle_Images/blob/2aebfa0d0ce386412d20ff885e76cacb8cf04ac1/Aug-2026/top1s.png)이 그 그림으로 보이지만, 댓글이 그림을 직접 가리키지는 않는다.

작성자는 에이전트를 보통 4시간에서 12시간 동안 자율로 돌린 뒤 짧은 요약을 받아 검토했고, 자신의 데이터 과학 직감으로 어디에서 신호를 더 찾을지와 무엇에 시간을 그만 쓸지를 알려 줬다고 했다.
결측에 관한 지시는 `have you studied the missing values and found features to deal with them?`이었고, 그 뒤 에이전트가 `studied the NANs for hours and found a new source of signal and improved their models`라고 밝혔다.
또 XGBoost, CatBoost, RealMLP가 서로 어떻게 다른지, 모형마다 특성과 탐색 방법을 어떻게 바꿔야 하는지, 어떤 조정 손잡이를 돌려야 하는지도 알려 줬다고 했다.

기억에 관해서는 4시간에서 12시간 단위 작업이 끝날 때마다 새 발견을 모두 적게 했고, 그 결과 `over 50 discovery.md files`가 쌓였다고 했다.
문맥은 `1M or more token context`를 쓰고 `never force a context compact`하며, 새 에이전트에게는 발견 파일을 모두 다시 읽게 한다고 했다.
에이전트가 며칠 동안 `tens of thousands of experiments`를 하므로 반복을 피하고 가능성이 남은 방향을 계속 탐색하려면 기억이 필요하다는 이유였다.
생각 예산은 돌파구가 필요하거나 중요한 탐색 분석과 특성 생성 탐정 작업을 할 때 최대로 두고, 일상적인 코딩에서는 줄인다고 했다.

#### XGBoost 특성: Ahmed Malik의 질문에 대한 답변

Ahmed Malik은 XGBoost를 약 `0.968`에서 `0.970`으로 올린 특성 생성 아이디어를 물었다.
작성자는 `It is more advanced versions of what we used in previous playground competitions`라고 답했고, GPT 5.6과 Fable 5가 옛 Playground write-up과 코드를 읽은 뒤 그 아이디어를 더 좋게 만들 수 있다고 말하고 실제로 그렇게 했다고 설명했다.
구체적인 내용은 새 Playground 대회에서 다시 통하는지 먼저 확인한 뒤 공개하겠다고 했다.

#### 특성의 효과: Masaya Kawamata의 질문에 대한 답변

Masaya Kawamata는 RealMLP와 XGBoost에서 효과적인 특성이 달랐는지, 어떤 특성 생성이 각 모형에 특히 효과적이었는지, ChatGPT Pro가 찾은 새 아이디어가 무엇인지 물었다.
작성자는 에이전트가 이렇게 좋은 단일 모형을 만들 줄 몰랐고, 반복해서 현재 결합을 이기는 단일 모형을 만들었다고 답했다.
에이전트는 단일 모형이 어디에서 틀리는지와 어떤 특성이나 구조가 어떤 유형의 패턴을 재현할 수 있는지를 계속 분석해 부족한 부분에 필요한 것만 계속 더했다고 설명했다.
구체적인 아이디어는 `for now i will try them on the new playground before publishing them`이라며 공개를 미뤘다.
두 모형에서 효과적인 특성이 달랐는지에 대한 직접 답변은 없었다.

#### 단일 모형과 결합의 관계: Tilii의 질문에 대한 답변

Tilii는 최고 단일 모형에서 최종 결합까지 이득이 `0.00033`뿐이라는 점에 놀랐다고 했다.
작성자는 큰 다양성 결합과 단일 모형 개선이 비슷한 일이라고 답했다.
결합은 새 모형이 다른 모형의 오류를 바로잡아 주기 때문에 효과가 있고, 에이전트의 단일 모형 개선도 모형이 어려워하는 지점을 연구해 특성이나 모형 조정으로 오류를 다루는 것이므로 `in both situations, we are addressing the errors`라는 설명이었다.
또 시간이 충분했다면 단일 모형이 전체 결합에 맞먹거나 넘어섰을 수 있고, 초기의 첫 단일 모형들은 실제로 380개 결합을 이겼다고 덧붙였다.

#### 지식 제공 방식: Naji의 질문에 대한 답변

Naji는 에이전트가 공개 노트북과 토론을 썼는지, 작성자의 분야 지식으로 안내했는지, 그리고 두 가지를 제한한 절제 실험을 제안했다.
작성자는 이 대회와 이전 대회의 공개 정보를 모두 쓰고 자신의 분야 지식도 쓴다고 답했다.
동시에 처음에는 아무 지식도 주지 않고 `Here's the data build a strong model`로 시작해 스스로 시도하게 하고, 그다음 단서를 조금 주고 다시 시도하게 하며, 마지막에 공개 지식과 자신의 지식을 주고 기존 아이디어와 합치게 한다고 했다.
지식 없이 만든 모형과 지식을 주고 만든 모형을 모두 결합에 더했다.
혼합 방식으로는 오래 일한 에이전트에게 새 에이전트용 시작 자료 묶음을 만들게 하되 공개 아이디어와 분야 지식 일부만 담게 했다.
작성자는 이를 `no knowledge, versus limited knowledge, versus all knowledge tricks`라고 불렀고 전부 시도한다고 했다.

#### 에이전트 운용 조언: delai50의 질문에 대한 답변

delai50은 이런 실행 환경을 만드는 조언을 물었다.
작성자는 에이전트를 어떻게 쓸지는 에이전트에게 물으라고 답했다.
먼저 GPT 5.6이나 Fable 5.1 같은 강한 에이전트 하나로 탐색 분석과 모형 구축을 직접 하게 해 경험을 쌓게 하는데, 1M 토큰 문맥에 쌓이는 대화, 생각, 도구 호출 이력이 작업 기억이라고 설명했다.
그다음 그 에이전트에게 다른 에이전트를 위한 자료 폴더, 지시, 시작 지시문을 만들게 했고, ChatGPT Pro와 NVIDIA Inference Hub의 여섯 모형은 이 방식으로 붙였다.
작업이 끝날 때마다 무엇을 했고 무엇을 배웠는지 Markdown 기록으로 남기게 했다.
GPT 5.6이 정체기에 들어 단일 모형을 못 올릴 때 Fable 5에게 그 기록을 읽게 하고 Fable 5가 알려 준 내용을 GPT 5.6에 전달하자 GPT 5.6이 Fable 5보다 좋은 단일 모형을 만들었다고 했다.

#### 그 밖의 작성자 답변

Lamaq Dahodwala가 흥미로운 추론 장면을 묻자, 작성자는 Fable 5가 주된 작업 관리자로서 다른 에이전트를 합류시키고 그 작업을 검토했으며 거짓말하거나 게으름 피우는 에이전트를 잡아냈다고 답했다.
Laurent Pourchot에게는 에이전트의 생각 흐름을 몇 시간씩 지켜봤고 그 능력이 자신을 훨씬 넘어서며, 자신은 여기저기서 안내만 했다고 답했다.
Optimistix에게는 에이전트 군단이 Playground에는 과하지만 NeuroGolf 우승 뒤 Playground에서 얼마나 잘하는지 궁금했다고 답했다.
Silvan Müller와 rampichino의 `왜 ML을 공부하는가`라는 회의에는 무엇을 배워야 할지 정하기 어려운 시기라는 데 동의했다.

### 제3자 댓글

다음은 작성자가 아닌 참가자의 수치이므로 우승 해법의 근거가 아니며 참고용으로만 적는다.

제3자 댓글: Tilii는 113개 구성원 결합에서 최고 단일 모형 `0.97030`, 결합 `0.97097`을 얻었다고 했다.
이 경우 결합 이득은 `0.00067`로 작성자의 `0.00033`보다 두 배 크다.

제3자 댓글: Georgy Mamarin은 자신의 노트북 측정값을 들어 결측 패턴 자체는 목표를 거의 설명하지 못한다고 했다.
결측 열 개수만으로는 `0.5017`, 어느 한 열의 결측 여부가 만드는 최대 차이는 기저율 `0.709` 대비 `0.0042`였고, 학습과 시험을 약 `0.566`으로 가르는 적대적 검증 신호는 결측에만 있어 무시해도 안전했다는 것이다.
반면 ryota517이 먼저 공개한 `일일 화면 시간은 소셜, 게임, 업무와 학습 시간의 합보다 작지 않다`는 제약은 생성기의 지문이라 결측 열을 남은 열의 산술로 부분 복원할 수 있고, `gaming_hours`와 `work_study_hours`가 세 화면 열 위에 더하는 신호의 `46%`가 두 열 모두 관측된 행만 남기면 사라진다고 했다.
결론은 무엇이 빠졌는지의 패턴은 목표를 말하지 않지만 빠진 값은 남은 값에서 복원되는 경우가 많다는 것이며, 작성자 에이전트의 발견이 이와 다른 기제인지 묻는 질문에 작성자 답변은 표시된 댓글에 없었다.
같은 댓글은 시드 42 분할을 확인한 111개 구성원으로 CV `0.969816`, 분할 규약을 확인하지 못한 47개를 더해 `0.970064`를 얻었다고 했다.

### 댓글 뒤에도 비공개인 항목

작성자 답변으로도 다음 항목은 여전히 공개되지 않았다.

- 최종 RealMLP와 XGBoost의 특성 목록, 기본 특성과 고급 특성의 정의, 결측 신호 특성의 형태
- RealMLP 튜닝에서 바뀐 설정값과 라이브러리 판본
- XGBoost 설정값과 XGBoost의 점수 분해
- 검증 분할 수, 분할표, 시드, 전처리 적합 범위
- 456개 구성원의 목록과 계열별 수량, 그림의 449개와의 차이 원인
- cuML 로지스틱 회귀의 입력 변환, 규제 강도, 가중치, 구성원 선택 기준, 결합기 검증 방식
- ChatGPT Pro가 찾았다는 새 특성 아이디어의 실체
- 50개가 넘는 발견 기록 파일, 지시문, 공유 지식 저장소
- 학습 장비, 총 학습 시간, 언어 모형 호출량과 비용

### 우리 해법과 비교 가능한 사실

다음은 [우리 최종 해법 복원](s6e8-our-final-solution.md)의 확정값과 맞대어 볼 수 있는 항목이며, 비교 자체는 분석 판정 C다.

분석 판정 C: 작성자의 RealMLP는 기본 특성 생성만으로 CV `0.9695`에 이르렀는데, 이는 우리 자체 최고 단일 구성인 결측 증강 Lookup-Transformer의 OOF AUC `0.9694062694182052`와 비슷하고, 튜닝과 고급 특성을 더한 `0.97070`은 우리 최고 단일 구성보다 `0.0013` 높다.
분석 판정 C: 두 CV는 분할 규약이 다를 수 있으므로 소수점 넷째 자리 차이를 그대로 비교하면 안 되지만, 자릿수 차이가 `0.001` 수준이라 단일 모형 격차 자체는 부정하기 어렵다.
분석 판정 C: 작성자의 결합기는 로지스틱 회귀이고 우리 결합기도 순위와 로짓을 입력으로 쓰는 L2 로지스틱 회귀이므로 결합기 종류는 같은 계열이지만, 우리 최종 314 구성의 중첩 OOF `0.9703843`과 작성자 결합의 CV `0.97098`은 검증 방식이 같은지 알 수 없어 직접 비교할 수 없다.
분석 판정 C: 작성자 결합의 단일 모형 대비 이득은 CV `+0.00028`, Private LB `+0.00031`인 반면, 우리 314 구성의 중첩 OOF는 자체 최고 단일 구성보다 `+0.00098` 높았으므로 우리는 결합에 더 기댔고 작성자는 단일 모형에 더 기댔다.
분석 판정 C: 작성자가 결측값 연구에서 새 신호 원천을 찾았다고 한 점은 우리의 제약식 기반 결측 보조 특성, XGBoost 결측 보조 특성, 결측 증강과 같은 방향이지만, 작성자 특성의 형태가 공개되지 않아 같은 신호인지는 판정할 수 없다.
분석 판정 C: 작성자는 공개 정보와 이전 대회 특성 생성을 에이전트에 전부 제공했고 Public LB 제출을 반복 확인했으나, 우리는 공개 점수를 판정에 쓰지 않았으므로 두 해법의 선택 절차는 공개 점수 의존도에서 다르다.

## 출처와 판본

### 우승 원문과 순위표

- [1등 write-up](https://www.kaggle.com/competitions/playground-series-s6e8/writeups/1st-place-distributed-intelligence-nvidia-infe), 게시 시각 `2026-08-31T23:59:52.153Z`, 조사 시점의 글 판본 갱신 시각 `2026-08-31T23:59:52.152Z`
- [연결 토론](https://www.kaggle.com/competitions/playground-series-s6e8/discussion/738592), 토론 식별자 `738592`, 2026-09-01 조사 시점 댓글 5개
- [우승 글 페이지의 댓글](https://www.kaggle.com/competitions/playground-series-s6e8/writeups/1st-place-distributed-intelligence-nvidia-infe), 2026-09-03 Chrome 재확인, Kaggle 표시 47개 중 40개 표시, 작성자 답변 11개
- [공식 최종 순위표](https://www.kaggle.com/competitions/playground-series-s6e8/leaderboard), Chris Deotte 1위, 점수 `0.97176`, 제출 시각 `2026-08-30T23:14:31.186Z`, 2026-09-03 재조회에서 2위 MILANFX `0.97123`
- [NVIDIA Build](https://build.nvidia.com/), 원문이 4단계 언어 모형 원천으로 직접 연결한 서비스

### 작성자가 글에 삽입한 그림

네 그림은 작성자의 `cdeotte/Kaggle_Images` 저장소 커밋 [`2aebfa0d0ce386412d20ff885e76cacb8cf04ac1`](https://github.com/cdeotte/Kaggle_Images/commit/2aebfa0d0ce386412d20ff885e76cacb8cf04ac1)에 고정돼 있다.
커밋 시각은 `2026-08-31T23:54:53Z`다.

- [에이전트와 공유 지식 구조](https://github.com/cdeotte/Kaggle_Images/blob/2aebfa0d0ce386412d20ff885e76cacb8cf04ac1/Aug-2026/agents.png)
- [결합 공개 점수와 모형 수 진행](https://github.com/cdeotte/Kaggle_Images/blob/2aebfa0d0ce386412d20ff885e76cacb8cf04ac1/Aug-2026/phases.png)
- [RealMLP와 XGBoost 단일 모형 경쟁](https://github.com/cdeotte/Kaggle_Images/blob/2aebfa0d0ce386412d20ff885e76cacb8cf04ac1/Aug-2026/top1s.png)
- [결합과 단일 모형 공개 제출 화면](https://github.com/cdeotte/Kaggle_Images/blob/2aebfa0d0ce386412d20ff885e76cacb8cf04ac1/Aug-2026/single_model.png)

### 작성자의 같은 대회 공개 노트북

판본 번호는 2026-09-01에 Kaggle API로 확인했고 소스 해시는 그 판본을 `kaggle kernels pull`로 내려받아 계산했다.

| 노트북 | 판본 | 소스 SHA-256 |
| --- | ---: | --- |
| [기초 탐색 분석](https://www.kaggle.com/code/cdeotte/basic-eda-smartphone-addiction) | 1 | `26a258981a9bcd1c61587be2275cbbddd5be894bfd1031b4e50d3b6bdc94a43d` |
| [XGBoost 입문](https://www.kaggle.com/code/cdeotte/simple-xgb-starter) | 1 | `958d80eab63f9ac627fdecbaab50b45a117cac4b459ab5865a7cb3a30e6aaefb` |
| [CatBoost 입문](https://www.kaggle.com/code/cdeotte/simple-cat-starter) | 2 | `434e805cee86d2e16ebdaa131b1cad6b5f51cf53e152aa667d6922c69ccdba80` |
| [신경망 입문](https://www.kaggle.com/code/cdeotte/simple-nn-starter) | 1 | `2f6e2ff792d3207d42bf9f13aa017cf0298ba3486e3d26267a2becc0fdeec41d` |

## 최종 판정

1등과 우리 해법의 차이를 논할 때 확실히 쓸 수 있는 비교축은 `RealMLP 단일 모형의 매우 높은 바깥쪽 예측 점수와 그 점수 분해`, `456개 모형을 cuML 로지스틱 회귀로 쌓은 대규모 결합`, `여러 에이전트가 독립 탐색 후 지식을 공유한 실험 처리량`, `단일 모형과 결합 각각의 Private LB`다.
반대로 `특정 새 특성`, `NVIDIA 기반 표 자료 추론`, `정확한 검증 계약`, `결합기의 입력 변환과 가중치`를 차이로 단정하면 공개 근거를 넘어선다.
후속 비교는 우리 저장소의 실제 자료, 특성, 모형, 검증, 결합 기록을 이 문서의 확정 항목과 미확정 항목에 맞춰 대조해야 한다.
