# 자체 구성원 순위 사분위 범위 전용 선형 결합 판정

## 결론

동결 후보는 **기각**이다.
후보 nested OOF AUC는 `0.9702403498605942`이고 대조군 대비 차이는 `-0.0000472599170831`다.
전체 개선 문턱 `+0.00002` 통과 여부는 `False`이고 바깥쪽 분할 5개 전부 개선 여부는 `False`다.
후보가 두 조건을 모두 통과하지 못하면 현행 최종 242개 결합을 유지한다.

## 기준 재현

대조군 nested OOF AUC는 `0.9702876097776773`로 동결값과 절대 차이 `0.000e+00`다.
대조군 예측 내용 해시는 `006ca009b1b8f115939747ca55c908755f3d999426f35cf90b8d00b66d7367cc`로 동결 해시와 같다.

## 분할별 결과

| 바깥쪽 분할 | 대조군 AUC | 후보군 AUC | 차이 | 개선 |
| ---: | ---: | ---: | ---: | :---: |
| 0 | 0.9697332661529522 | 0.9696902397501850 | -0.0000430264027672 | 아니요 |
| 1 | 0.9704381694607899 | 0.9704052781408580 | -0.0000328913199319 | 아니요 |
| 2 | 0.9703637806523540 | 0.9702991705775861 | -0.0000646100747679 | 아니요 |
| 3 | 0.9708882903449186 | 0.9708295863548213 | -0.0000587039900973 | 아니요 |
| 4 | 0.9700145394936390 | 0.9699770632054926 | -0.0000374762881464 | 아니요 |

## 설명 지표

확인 구간에는 전체 `177476`행, 목표값 0 `62823`행, 목표값 1 `114653`행이 들었다.
확인 구간의 표준 순위 손실 부담 평균 감소는 `-0.0001622760677488`다.
확인 구간의 결측 보정 순위 손실 부담 평균 감소는 `-0.0001426244937099`다.
결측 보정 OOF AUC는 대조군 `0.9711558537184679`, 후보군 `0.9711111651534262`다.
이 지표들은 설명 자료이며 채택 판정을 바꾸지 않는다.

## 전체 자료 적합과 시험 자료 적용

전체 OOF의 확인 구간 학습 행은 `172729`개다.
시험 자료에서는 `71868`행에 구간 전용 결합 순위를 적용했다.
후보 시험 예측 내용 해시는 `d5a5af82e14200af267be9aaf9464bcd700c855a68295b4e28fb6ba369debae7`다.
제출 파일은 만들지 않았고 MLflow, 후보 풀과 champion도 바꾸지 않았다.

## 산출물

후보 nested OOF 예측 내용 해시는 `4c2eea06e89a945193da05fe86497f70b14ec26349a19101c213170efb521394`다.
대용량 OOF와 시험 예측은 `run-logs/issue467`에 두고 커밋하지 않는다.
기계 판독 근거는 `docs/research/own-member-iqr-specialized-stack-evidence.json`이다.
