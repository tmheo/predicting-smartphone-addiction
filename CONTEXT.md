# 스마트폰 중독 예측

Kaggle Playground Series S6E8의 스마트폰 중독 여부 예측 과제에서 사용하는 핵심 용어를 정의한다.

## 조사 대상

### 합성 생성 포렌식

**후보 생성 과정**:
원본 프록시에서 설명변수와 목표값을 생성하고 산술 제약 보정과 결측 주입까지 수행했을 가능성이 있는 일련의 절차.
특정 합성 자료 라이브러리 이름만이 아니라 목표값 생성 순서와 생성 후 보정까지 포함한다.
_Avoid_: 생성기, 생성 알고리즘

**생성 지문**:
후보 생성 과정을 구분하고 예측 표현으로 옮길 수 있는 값 눈금, 조건부 분포, 제약 위반, 결측 또는 목표값 확률 표면의 관측 가능한 통계적 흔적.
후보 생성 과정의 제품명이나 정확한 구현을 맞혔다는 뜻으로 사용하지 않는다.
_Avoid_: 흔적, 패턴, 생성기 특성

**원본-합성 변환**:
원본 프록시와 대회 자료 사이에서 조건부 확률, 밀도, 값 눈금, 산술 제약 또는 결측 구조가 달라진 방식.
프록시가 실제 생성 원본이라는 전제를 포함하지 않는다.
_Avoid_: 데이터 변환, 생성 규칙

**분석 대상 노트북**:
조사 시점에 대회 Code 탭에서 공개 중이고 득표 수가 10개 이상인 노트북의 최신 공개 판본.
_Avoid_: 득표 노트북, vote가 있는 노트북, 인기 노트북

**재사용 가능한 인사이트**:
출처 코드와 검증 근거를 확인할 수 있고 이 저장소의 후속 실험 후보로 옮길 수 있는 데이터, 검증, 전처리, 특성 생성, 모형 또는 앙상블에 관한 발견.
_Avoid_: 팁, 아이디어

**스레드 장부**:
디스커션 종합 문서에 반영된 스레드의 전수 목록으로, 스레드별 확인 시점의 코멘트 수와 재방문 여부를 함께 기록한 것.
_Avoid_: 읽은 목록

**재방문 스레드**:
새 코멘트가 달렸는지 증분 회차마다 코멘트 수 변화로 다시 확인하는 고가치 스레드.

**증분 회차**:
직전 스냅샷 이후 올라온 새 스레드와 재방문 스레드의 새 코멘트를 종합 문서에 반영하는 1회 실행 단위.

## 실험 운영

**실험 프로그램**:
실험 관찰 기능이 준비된 뒤부터 최종 제출 후보 두 개를 선정할 때까지 이어지는 전체 실행 지도.
_Avoid_: 전체 실험 리스트, 실험 계획

**원격 실험 실행 환경**:
로컬 장비 밖의 GPU 자원에서 실험 실행을 수행하고 실행 기록 묶음으로 결과를 회수하는 데 필요한 계산 자원, 실행 세션, 저장 수단의 묶음.
주 실행 환경과 사용 불가 시 전환할 예비 실행 환경을 함께 가리킨다.
_Avoid_: GPU 서비스, 클라우드 환경

**원격 자원 제어 경로**:
원격 실행 작업에 사용할 매물을 찾고 계산 자원을 생성한 뒤 상태와 비용을 확인하며 마지막에 자원 삭제와 과금 중지를 확인하는 공급자 제어 수단.
계산 자원 안에서 명령을 실행하는 실행 접속 경로와 구분한다.
_Avoid_: 접속 방법, 로그인 방식

**실행 접속 경로**:
생성된 원격 계산 자원에 입력을 전송하고 명령을 실행하며 결과를 회수하는 통신 수단.
공급자 계정과 계산 자원을 관리하는 원격 자원 제어 경로와 구분한다.
_Avoid_: 원격 자원 제어, 서비스 로그인

**원격 실행 작업**:
새로 만든 원격 계산 자원 하나에서 실험 실행 하나를 수행하고 묶음 반입과 정리까지 추적하는 운영 단위.
확정 재검증의 여러 시드는 하나의 원격 실행 작업에 속하지만 다른 설정이나 실행 단계는 별도 작업으로 분리한다.
_Avoid_: 원격 세션, 원격 실험

**원격 실행 명세**:
원격 실행 작업의 커밋, 설정과 잠금 파일, 입력, 실행 단계, 서비스, GPU와 컨테이너 환경을 내용 해시와 승인 조건으로 고정한 불변 기록.
_Avoid_: 실행 설정, 환경 설정

**원격 실행 장부**:
원격 실행 작업별 명세, 비용 예약, 공급자 자원, 상태 변화와 정리 확인을 트랜잭션으로 보존하는 로컬 기록 원본.
_Avoid_: 상태 파일, 작업 목록

**비용 예약**:
완료되지 않은 원격 실행 작업에서 발생할 수 있는 최대 비용을 월 비용 한도에서 미리 사용한 것으로 간주하는 임시 배정.
작업을 마치면 실제 비용으로 바꾸고 남은 금액을 해제한다.
_Avoid_: 예상 비용, 비용 견적

**독립 종료 예약**:
실행 접속 경로나 계산 자원 안의 프로세스에 의존하지 않고 공급자 제어 경로에 등록하는 계산 자원 종료 약속.
_Avoid_: 자동 종료, 실행 시간 제한

**공급 환경 장애**:
공급자 계산 자원이나 원격 자원 제어 경로의 문제로 원격 실행 작업을 시작하거나 계속할 수 없게 된 상태.
프로그램, 자료, 명세 또는 GPU 메모리 부족으로 생긴 실패와 구분한다.
_Avoid_: 실행 오류, 작업 실패

**입력 전송 묶음**:
원격 실행 명세가 열거한 비커밋 입력과 각 파일의 내용 해시를 함께 담아 로컬에서 원격 계산 자원으로 옮기는 자기완결 파일.
실행 결과를 반대 방향으로 옮기는 실행 기록 묶음과 구분한다.
_Avoid_: 데이터 압축 파일, 업로드 파일

**원격 결과 완료**:
회수한 실행 기록 묶음이 모든 묶음 반입 검증을 통과해 로컬 실행 저장소에 정상 실험 실행으로 재생된 상태.
원격 학습 완료나 묶음 전송만으로는 이 상태가 되지 않는다.
_Avoid_: 원격 실행 성공, 다운로드 완료

**대회 전용 SSH 접속 키**:
하나의 대회에서 같은 공급자의 여러 원격 실행 작업이 함께 사용하는 접속 신원.
공급자별로 분리하며 개별 작업보다 오래 유지하고 대회 또는 해당 공급자 사용 종료와 함께 수명을 끝낸다.
_Avoid_: 공용 SSH 키, 작업용 SSH 키

**원격 운영 종료**:
원격 실행 작업의 결과 또는 실패가 확정되고 계산 자원, 저장 공간과 해당 작업의 수명을 따르는 접근 수단을 정리한 뒤 공급자 과금 속도가 0임을 확인한 상태.
대회 전용 SSH 접속 키처럼 작업보다 긴 수명을 가진 접근 수단은 포함하지 않는다.
_Avoid_: 인스턴스 종료, 작업 완료

**실험 트랙**:
하나의 질문을 여러 설정 변형으로 검증하고 후속 실행 여부까지 결정하는 실험 실행의 묶음.
_Avoid_: 실험 묶음, 실험군

**진입 진단**:
champion과 성능을 겨루지 않고 측정 결과에 따라 조건부 실험 트랙을 열거나 닫는 검사.
_Avoid_: 사전 실험, 탐색 실험

**동등 단계 기준 실행**:
진입 진단에서 challenger와 같은 입력 내용, fold 파일과 번호, 시드, 검증 행과 목표값, 피처 계획, 실행 의존성 판본을 공유하고 허용한 모델 설정 축만 다른 champion 실행.
저장된 진단 JSON과 검증 예측을 함께 재현한 뒤에만 challenger의 fold AUC와 짝지어 비교한다.
_Avoid_: champion fold 점수, 기준 숫자, 3시드 평균 fold 기준

**전처리 기준 집합**:
목표값을 참조하지 않는 어휘, 분위 변환 같은 모형 전처리 상태를 맞추기 위해 사용하는 설명변수 행 집합.
fold별 모형 학습 자료와 구분하며, 실험 계약이 허용할 때 train과 test 설명변수를 함께 포함할 수 있다.
_Avoid_: 전이 학습 자료, 학습 자료

**후보 저장소**:
검색형 모델이 query별 문맥을 고를 때 참조하는 outer 학습 fold의 행 표현과 라벨 집합.
검증 행과 검증 라벨은 포함하지 않으며, 학습 query는 자기 행을 검색 결과에서 제외한다.
_Avoid_: 메모리 뱅크, 검색 데이터베이스

**다양성 구성원**:
단독 OOF 성능이 champion보다 낮아도 누출 없는 앙상블 평가를 개선해 후보 풀에 유지하는 모델 실행.
_Avoid_: 약한 모델, 보조 모델

**실험 실행**:
검증된 설정 파일 하나로 시작해 데이터 적재, 특성 생성, 학습, 평가, 산출물 저장을 거쳐 끝나는 MLflow run 하나.
_Avoid_: 학습, MLflow 기록

**복구 경계**:
중단된 실험 실행을 이어갈 때 다시 실행하지 않고 재사용할 수 있는 최소 완료 단위.
_Avoid_: 재개 단위, 저장 단위

**피처 계획**:
실험 실행 하나가 학습할 컬럼 전체를, 컬럼 제공자들의 순서 있는 목록으로 선언하고 검증하는 구성 요소.
적용 단계 순서와 placebo 상시 포함을 불변식으로 소유한다.
_Avoid_: 피처 파이프라인, 피처 셋

**컬럼 제공자**:
적용 단계(dataset-wide / row-wise / fold-fit)와 산출 컬럼 이름, 타깃 참조 여부를 정적으로 선언하고 그 컬럼을 만들어내는 피처 계획의 단위.
_Avoid_: 피처 함수, 트랜스포머(fold-fit 한정 용어로만 유지)

**모델 계열**:
`model.kind` 하나로 식별되는 학습기 구현.
파라미터 해석, 시드 적용, early stopping 등 학습 세부를 소유하고 cv 루프에는 예측과 importance만 돌려준다.
_Avoid_: 모델 종류, 알고리즘

**표 합성곱망**:
의미가 정의되지 않은 열 나열 위에 1차원 합성곱을 적용하는 모델 계열(`tab_cnn`).
열 순서에 의미가 없으므로 합성곱의 기여는 제거 대조로 확인한 뒤에만 인정한다.
_Avoid_: CNN 모델, TabCNN

**스칼라 token Transformer**:
수치 스칼라 입력을 여러 표현으로 token화해 attention으로 섞는 모델 계열(`scalar_token_transformer`).
범주형 token만 attention에 넣는 원 논문 구조와 다르므로 TabTransformer라고 부르지 않는다.
_Avoid_: TabTransformer

**조회 어휘 미등록값**:
Lookup-Transformer의 각 fold에서 결측은 아니지만 해당 학습 fold의 정확값 어휘에 없어 미등록 식별자로 처리되는 검증 또는 테스트 값.
_Avoid_: UNK, 미관측값

**실행 저장소**:
완료된 실험 실행의 사실(params·metrics·tags)과 산출물을 읽고, 뒤늦은 주석(태그·지표 추가)을 남기는 유일한 통로.
MLflow는 이 저장소의 adapter 하나일 뿐이며, 기록 규약의 의미 해석(metric 이름 파싱 등)은 판정 쪽 소관이다.
_Avoid_: run store, MLflow 래퍼

**실행 기록 묶음**:
완료된 실험 실행의 기록 원형과 산출물 전체를 manifest와 함께 zip 하나로 사영한 자기완결 파일.
Kaggle GPU 실행처럼 로컬 밖에서 끝난 실행을 로컬 실행 저장소로 옮기는 운반 단위다.
_Avoid_: bundle, 익스포트 파일

**묶음 반입**:
실행 기록 묶음을 입력 해시·출처(commit과 config 동일성) 검증과 시드별 OOF 재채점을 거쳐 로컬 실행 저장소에 정상 실험 실행으로 재생하는 절차.
원격이 주장한 지표는 기록하지 않으며, 검증을 하나라도 통과하지 못하면 반입하지 않는다.
_Avoid_: 임포트, 결과 동기화

**실행 관찰 기록기**:
실험 실행 하나에 붙어 MLflow 실행의 생성부터 종료까지 수명주기를 소유하고, 진행 기록, 생존 신호, 실행 로그, 오류 기록, 종료 처리를 한곳에서 담당하는 구성 요소.
교차 검증은 이 기록기에 일어난 일을 통지할 뿐 MLflow를 직접 알지 못한다.
_Avoid_: 트래커, 로거, 옵저버

**진행 기록**:
실험 실행의 현재 단계, 시드와 fold 진행률, 단계별 소요 시간과 중간 평가값을 구조화해 남긴 관찰 정보.
_Avoid_: 진행 로그, 상태 메시지

**생존 신호**:
실험 실행이 살아 있음을 보이기 위해 60초마다 남기는 경과 시간 기록.
마지막 신호 시각으로 실행의 중단 여부를 판정한다.
_Avoid_: 하트비트, heartbeat

**실행 로그**:
실험 실행 중 로컬 파일에 이어 쓰고 완료 또는 실패 후 MLflow 산출물로 보존하는 전체 텍스트 기록.
_Avoid_: 콘솔 로그, MLflow 로그

**결과 요약**:
원본 예측 및 특성 중요도 산출물과 함께 저장해 MLflow UI에서 별도 코드 실행 없이 읽는 표와 그림.
_Avoid_: 결과물, 리포트

**요약 생성기**:
실험 실행의 최종 결과와 단계별 소요 시간을 입력받아 결과 요약의 표와 그림을 만드는 구성 요소.
요약 생성이 실패하면 해당 실험 실행은 실패로 처리한다.
_Avoid_: 리포트 생성기

**스테일 실행**:
강제 종료로 종료 처리가 수행되지 못해 MLflow에는 진행 중으로 남았지만 실제 프로세스는 사라진 실험 실행.
마지막 활동 시각과 프로세스 부재를 함께 확인해 판정하고, 정리 절차가 강제 종료 상태로 종료한다.
_Avoid_: 좀비 실행, 고아 실행

**마일스톤 제출**:
baseline과 각 피처 노력이 끝나는 시점에만 수행하는 Kaggle 제출로, public 점수가 CV(OOF) 점수와 같은 방향인지 확인하는 건전성 점검 용도로만 쓴다.
public 점수는 실험 채택 여부의 판단 기준이 아니다.
_Avoid_: 리더보드 확인, 점수 보러 제출

**champion**:
채택된 실험 가운데 현재 최고 성능으로 확정되어, 새 실험의 개선 판정 기준이 되는 run.
_Avoid_: 기준 모델, 최고 기록

**challenger**:
champion과 비교해 개선 판정을 받는 새 실험 run.
판정을 통과해 채택되면 새 champion이 된다.
_Avoid_: 도전 모델

**실행 후보**:
현재 자료 규모, 원격 실행 환경, 라이선스와 재현 근거를 충족해 구체적인 사전 진단과 중단 조건을 붙일 수 있는 미실험 모델 계열.
_Avoid_: 유망 모델, 추천 모델

**실행 후보 포트폴리오**:
서로 다른 작동 원리를 대표하는 실행 후보를 champion 교체와 후보 풀 다양성의 주목표, 우선순위, 진입 진단과 중단 조건으로 묶은 선정 결과.
_Avoid_: 후보 목록, 모델 쇼핑 리스트

**관찰 후보**:
작동 원리나 연구 결과는 유망하지만 구현, 규모, 계산 자원 또는 사용 조건의 근거가 부족해 아직 실험 프로그램에 편입하지 않는 모델 계열.
_Avoid_: 보류 모델, 나중에 볼 모델

**조건부 승격 관찰 후보**:
부족한 사용 조건이나 계산량 게이트가 구체적으로 식별되어, 그 조건을 충족하면 새 광역 조사 없이 실행 후보로 다시 판정할 수 있는 관찰 후보.
_Avoid_: 예비 실행 후보, 차순위 후보

**근거 갱신 관찰 후보**:
공식 구현, 사용 조건이나 현재 자료 규모의 연구 근거가 부족하여, 새 근거가 공개된 뒤에만 다시 판정하는 관찰 후보.
_Avoid_: 장기 보류 후보, 관심 모델

**개선 판정**:
실험 결과의 채택 여부를 정하는 규칙.
특성·단일 모델, 다양성 구성원, 앙상블 세 계열로 나뉘며, 계열별 증거와 문턱은 판정 계약(ADR 0001)을 따른다.
_Avoid_: 성능 비교

**판정 불가**:
증거 부족이나 전제 위반으로 개선 판정을 수행할 수 없는 상태.
미달(판정은 됐으나 탈락)과 구분되는 예외로 보고하며, CLI가 종료 메시지로 번역한다.
_Avoid_: 판정 실패, 판정 에러

**스크리닝**:
고정된 단일 시드로 후보를 빠르게 거르는 1차 판정.
통과는 채택이 아니며 확정 재검증의 대상이 될 자격만 준다.
_Avoid_: 예비 실험, 1차 실험

**대리 스크리닝**:
느린 champion 모델 계열의 공식 스크리닝에 앞서, 빠른 모델 계열의 동일 조건 기준 실행과 후보 실행을 짝지어 특성 후보를 거르는 선별 절차.
대리 모델 안에서 OOF AUC 변화량과 새 특성 중요도를 판정하며, 통과하더라도 champion 모델 계열의 스크리닝과 확정 재검증을 대신하지 않는다.
_Avoid_: LightGBM 검증, 빠른 스크리닝

**약식 검증**:
대량 후보의 순위만 매기기 위해 검증 fold 하나와 가벼운 학습 설정으로 실행하는 선별 절차.
점수는 약식 기준 실행 대비 차이로만 읽으며 후보 선별에만 쓰고, 스크리닝과 확정 재검증 등 champion 판정에는 쓰지 않는다.
_Avoid_: 프리스크리닝, 라이트 검증

**약식 기준 실행**:
champion의 피처 구성을 약식 검증과 같은 조건으로 실행해, 모든 약식 후보 점수의 비교 기준이 되는 실행.
_Avoid_: 약식 베이스라인

**확정 재검증**:
고정된 3시드 평균본으로 채택을 확정하는 2차 판정.
_Avoid_: 파이널 체크

**경계 구간**:
확정 재검증에서 시드 평균본 개선 폭이 확정 문턱 이상, +0.0002 미만(+0.00002 이상 +0.0002 미만)인 구간.
이 구간에서만 fold 승리(5개 중 3개 이상)를 추가 게이트로 요구하고, 그 외 구간의 fold 승리 수는 보조 증거로 기록만 한다.
_Avoid_: 그레이존, 애매한 구간

**시드 평균본**:
고정된 시드들로 각각 학습한 예측을 평균해 하나로 만든 실행본.
다중 시드 실행의 OOF AUC는 이 평균 예측을 채점한 값이다.
_Avoid_: 시드 앙상블

**fold 내 초기화 평균**:
파이프라인 시드와 fold를 고정한 채 초기화 시드만 다른 같은 모델 계열의 예측을 fold 안에서 평균하는 모델 설정.
확정 재검증의 시드 평균본과 구분하며, 파이프라인 시드 하나당 fold 예측 하나를 만든다.
_Avoid_: fold 내 시드 평균, 내부 시드 앙상블

**시드 범위**:
한 실험 실행에서 시드별 OOF AUC의 최댓값과 최솟값의 차이로 잰 시드 안정성 값.
_Avoid_: 시드 편차, 시드 분산

**평균화 이득**:
시드 평균본 OOF AUC에서 시드별 OOF AUC의 산술 평균을 뺀 값.
실행 간 우열의 근거가 아니라 시드 간 예측 다양성을 읽는 기술 통계다.
_Avoid_: 시드 앙상블 이득, 평균 효과

**시간순 최고선**:
실행 시작 시각 순서로 시드 평균본 OOF AUC의 누적 최고값을 이은 선.
실험 프로그램의 진행을 보여주는 이야기 재료이며 개선 판정의 근거가 아니다.
_Avoid_: 리더보드 곡선, 성능 추이

**전이 분석**:
Public 점수가 기록된 실행에서 그 실행 자신의 OOF AUC와 Public AUC를 짝지어 같은 방향인지 살피는 건전성 점검 구획.
표본은 유망 실행만 선택적으로 제출된 결과이므로 표본 안의 관찰 서술만 허용하고, 미제출 실행이나 미래 실행으로 일반화하지 않는다.
_Avoid_: 리더보드 상관 분석, Public 검증

**전이 표시**:
전이 분석이 구성별로 내보내는 세 값 표시로, 값은 전이 일치, 역전 관찰, 미제출 중 하나다.
격차 수치는 표시에 포함하지 않으며, 다음 실험 권고가 쓸 수 있는 유일한 전이 신호다.
_Avoid_: 전이 점수, 전이 지표

**다음 실험 권고**:
회고 노트북이 대표 구성과 앙상블 대표 실행에 확장, 재검증, 보류, 중단 중 하나로 붙이는 다음 실험 우선순위 표시.
채택 판정이 아니며 champion·풀 장부를 변경하지 않고, 재검증은 증거 문제, 나머지 셋은 성능·전망 판단이라는 축 분리를 유지한다.
_Avoid_: 채택 권고, 실험 판정

**확장 후보권**:
시드 평균본 OOF AUC가 champion − 0.001 이상인 다음 실험 권고 전용 구간.
개선 판정의 문턱이 아니며 확장과 재검증을 가르는 우선순위 경계로만 쓴다.
_Avoid_: 확장 구간, 상위권

**후보 풀**:
앙상블 구성 후보로 유지하는 시드 평균본 실행의 장부.
진입과 탈락은 개선 판정의 다양성 계열 규칙을 따른다.
_Avoid_: 모델 리스트

**진입 하한**:
후보 풀 진입의 최소 성능 조건으로, 시드 평균본 OOF AUC가 진입 시점 champion − 0.01 이상이어야 한다.
진입 시점에만 적용하고 champion 갱신 때마다 재심사하지 않는다.
_Avoid_: 컷라인, 최소 점수

**중복 게이트**:
풀 내 최근접 구성원과 OOF 예측의 스피어만 순위 상관이 0.998 이상이면 중복으로 보고 성능이 높은 쪽만 유지하는 판정.
상관은 중복 제거 전용이며, 탈락은 조기 확정이고 교체는 진입 여부를 정하지 않는다.
_Avoid_: 상관 필터, 유사도 체크

**기여 참고값**:
표준 평가 앙상블의 OOF AUC에서 해당 구성원을 제외했을 때의 변화를 기록한 값.
균등 순위 평균 하나의 특성만 보여 주므로 후보 진입이나 제거 판정에는 쓰지 않는다.
_Avoid_: 기여 판정, ablation, 기여도 분석

**후보 풀 일괄 재심사**:
후보 풀 전체의 계보와 예측 무결성을 확인하고 정확·순위 중복을 제거하는 절차.
검사를 통과한 후보는 균등 순위 평균 기여의 부호와 관계없이 모두 nested OOF 평가에 넘긴다.
진입 근거는 진입 시점 스냅샷이므로 재심사 때 갱신하지 않는다.
_Avoid_: 풀 청소, 일괄 삭제

**기여 영점 대역**:
고정 난수의 독립 순위 예측과 기존 구성원의 정확 복제를 표준 평가 앙상블에 각각 추가해 측정한 기여 변화의 최솟값부터 최댓값까지의 구간.
무정보와 단순 재가중만으로 생기는 측정 대역이며, 후보 진입이나 제거 판정에는 쓰지 않는다.
_Avoid_: 노이즈 플로어, 유의성 문턱

**채택 자격**:
장부(champion·후보 풀)에 오르는 실행이 갖춰야 하는 기록 조건.
3시드 평균본이고, git_dirty가 아니고, 커밋된 folds와 sha256이 일치해야 하며, 검사의 소스는 판정 module 한 곳이다.
_Avoid_: 등록 조건, 제출 자격

**판정 장부**:
개선 판정의 결과를 git에 커밋해 남기는 기록의 원본으로, champion 장부(artifacts/champion.yaml)와 풀 장부(artifacts/pool.yaml) 두 장이다.
mlflow.db 없이도 "무엇이 champion인가"와 "무엇이 풀에 있는가"를 git 이력으로 재구성할 수 있게 하며, YAML 모양의 해석과 기록은 장부 module 한 곳이 소유한다.
_Avoid_: yaml 파일, dict 장부

**풀 장부**:
후보 풀의 판정 장부로, 구성원별 성능·시드·진입 사유와 진입 근거를 진입 순서대로 담는다.
_Avoid_: pool.yaml dict, 멤버 리스트

**진입 근거**:
풀 진입 판정 당시의 champion(run과 OOF AUC)과 판정 값(하한 여유 폭, 최근접 상관, 앙상블 기여)을 담아 풀 장부에 남기는 진입 시점 스냅샷.
champion이 교체되어도 재평가하거나 갱신하지 않으며, 당시 champion의 OOF AUC를 함께 기록하므로 그 run이 장부에서 사라져도 스스로 완결된다.
_Avoid_: evidence dict, 진입 증거

**표준 평가 앙상블**:
후보 풀 전체의 순위 평균으로 만든, 구성원의 제외 전후 기여를 재는 기준 앙상블.
_Avoid_: 베이스라인 앙상블

**nested OOF**:
outer fold마다 나머지 fold의 OOF만으로 구성원 선택과 가중치를 정해 만든, 선택 편향 없는 앙상블 OOF.
_Avoid_: 이중 교차검증

**nested OOF 평가**:
후보 풀의 시드 평균본 전체를 구성원으로, outer fold마다 나머지 fold의 OOF로 결합 전략을 학습해 nested OOF를 채점하고 전략 간 비교와 계열 3 판정까지 내는 측정 절차.
평가 자체는 stdout 리포트로 내고, 요청 시 최선 전략의 결과 하나를 파생 앙상블 실행으로 MLflow에 기록한다.
_Avoid_: 앙상블 실험, 스태킹 평가

**파생 앙상블 실행**:
nested OOF 평가의 최선 전략 결과를 MLflow에 남기는 파생 기록으로, 구성원 계보와 nested OOF 예측, 직전 기록 대비 증분을 담는다.
실험을 새로 실행한 run이 아니라 기존 구성원 실행에서 파생된 기록이며, 시점별 풀 전체의 OOF AUC를 MLflow 한 곳에서 추적하는 수단이다.
_Avoid_: 앙상블 실험 run, nested run

**결합 전략**:
구성원 OOF 예측 행렬을 하나의 예측으로 결합하는 fit/predict/summary 계약의 adapter.
순위·logit 변환 같은 전처리는 전략이 소유하고, 평가기는 float64 원시 예측만 건넨다.
_Avoid_: 블렌더, 결합 방법

**결측 구간 결합 전략**:
원시 특성의 결측 개수를 0-1개, 2-3개, 4개 이상으로 나누고 각 구간의 구성원 결합을 따로 학습하는 결합 전략.
구간 경계는 목표값이나 평가 점수로 고르지 않는다.
_Avoid_: 결측별 스태킹, 구간별 2단 결합

**전체 자료 재학습**:
후보 풀 구성원의 설정과 시드를 유지하면서 훈련 자료 전부로 모델 하나를 다시 맞추고 시험 예측을 만드는 절차.
fold-fit 피처의 학습 행 표현은 내부 OOF 규율을 유지하며, 반복형 모델은 재학습 계획에 고정된 학습 길이를 사용한다.
_Avoid_: 최종 학습, 전체 학습

**재학습 계획**:
전체 자료 재학습에 사용할 후보 실행, 설정 파일, 시드, 학습 길이와 그 출처, 결합 가중치를 후보 풀 내용 해시와 함께 고정한 장부.
_Avoid_: 재학습 설정, 실행 목록

**학습 길이**:
조기 종료 없이 전체 자료 재학습을 끝낼 반복 횟수 또는 epoch 수.
바깥쪽 분할에서 관측한 최적 길이의 시드별 중앙값에 1.25를 곱해 사사오입한 값을 원칙으로 한다.
_Avoid_: 최적 반복 수, 최대 반복 수

**모델 수 가중 혼합**:
5-fold 시험 예측 평균과 전체 자료 예측을 각 평균에 들어간 학습 모델 수인 5:1로 섞어 여섯 모델에 같은 가중치를 주는 방식.
_Avoid_: 5:1 블렌딩, CV와 전체 예측 평균

**플라시보 피처**:
무작위 값에 실제 열과 같은 결측 패턴을 입혀 모델 입력에 상시 포함하는 잡음 피처.
중요도가 이보다 낮은 피처는 잡음으로 본다.

**fold-fit 피처**:
각 CV fold의 훈련 데이터로만 학습(fit)하고 그 fold의 검증 데이터와 테스트에 적용(transform)하는 피처.
타깃 인코딩처럼 타깃을 참조하는 피처가 검증 라벨을 누수 없이 쓰기 위한 구분이다.
_Avoid_: fold-aware 피처, 폴드 내 피처

**placebo 카나리아**:
플라시보 피처를 후보 피처와 같은 방식으로 타깃 인코딩해 모델 입력에 함께 넣는 누수 자가 검출 컬럼.
구현이 올바르면 중요도가 잡음 수준에 머물고, 평균표에 검증 라벨이 새면 라벨을 암기해 중요도가 치솟는다.
개선 판정의 대상이 아니며, 이 컬럼의 중요도가 플라시보 피처보다 낮아야 해당 run이 유효하다.
_Avoid_: 누수 체크 컬럼

**계열 무관 중요도**:
모든 모델 계열이 모델에 맞는 방식으로 계산해 gain 축으로 환산해 돌려주는 피처 중요도.
플라시보 게이트와 결과 요약이 읽는 중요도는 이것이며, gain importance가 없는 계열은 검증 fold permutation importance(AUC 하락 폭)를 쓴다.
환산은 실행 시드로 결정적이어야 하고, 환산 방식의 선택은 모델 계열이 소유한다.
_Avoid_: 환산 gain, importance 규약

**플라시보 게이트**:
피처의 평균 계열 무관 중요도를 플라시보 피처의 평균 기준값과 비교해 통과 여부를 정하는 판정 게이트.
카나리아는 기준값 미만이어야 run이 유효하고, 새 피처는 기준값을 넘어야 기여로 인정한다.
기준값이 미기록이면 실패이며, 게이트 규칙의 소스는 판정 module 한 곳이다.
tracking의 경고와 결과 요약의 플라시보 비교는 게이트가 아니라 관찰·표시다.
_Avoid_: importance 필터, 플라시보 체크
