# 스마트폰 중독 예측

Kaggle Playground Series S6E8의 스마트폰 중독 여부 예측 과제에서 사용하는 핵심 용어를 정의한다.

## 조사 대상

### 합성 생성 포렌식

**후보 생성 과정**:
원본 프록시에서 설명변수와 목표값을 생성하고 산술 제약 보정과 결측 주입까지 수행했을 가능성이 있는 일련의 절차.
특정 합성 자료 라이브러리 이름만이 아니라 목표값 생성 순서와 생성 후 보정까지 포함한다.
_Avoid_: 생성기, 생성 알고리즘

**생성 지문**:
후보 생성 과정을 구분하고 예측 표현으로 옮길 수 있는 값 눈금, 조건부 분포, 제약 위반, 결측 또는 목표값 확률 표면의 관측 가능한 통계적 흔적.
후보 생성 과정의 제품명이나 정확한 구현을 맞혔다는 뜻으로 사용하지 않는다.
_Avoid_: 흔적, 패턴, 생성기 특성

**원본-합성 변환**:
원본 프록시와 대회 자료 사이에서 조건부 확률, 밀도, 값 눈금, 산술 제약 또는 결측 구조가 달라진 방식.
프록시가 실제 생성 원본이라는 전제를 포함하지 않는다.
_Avoid_: 데이터 변환, 생성 규칙

**분석 대상 노트북**:
조사 시점에 대회 Code 탭에서 공개 중이고 득표 수가 10개 이상인 노트북의 최신 공개 판본.
_Avoid_: 득표 노트북, vote가 있는 노트북, 인기 노트북

**재사용 가능한 인사이트**:
출처 코드와 검증 근거를 확인할 수 있고 이 저장소의 후속 실험 후보로 옮길 수 있는 데이터, 검증, 전처리, 특성 생성, 모형 또는 앙상블에 관한 발견.
_Avoid_: 팁, 아이디어

**스레드 장부**:
디스커션 종합 문서에 반영된 스레드의 전수 목록으로, 스레드별 확인 시점의 코멘트 수와 재방문 여부를 함께 기록한 것.
_Avoid_: 읽은 목록

**재방문 스레드**:
새 코멘트가 달렸는지 증분 회차마다 코멘트 수 변화로 다시 확인하는 고가치 스레드.

**증분 회차**:
직전 스냅샷 이후 올라온 새 스레드와 재방문 스레드의 새 코멘트를 종합 문서에 반영하는 1회 실행 단위.

## 실험 운영

**실험 프로그램**:
실험 관찰 기능이 준비된 뒤부터 최종 제출 후보 두 개를 선정할 때까지 이어지는 전체 실행 지도.
_Avoid_: 전체 실험 리스트, 실험 계획

**실험 트랙**:
하나의 질문을 여러 설정 변형으로 검증하고 후속 실행 여부까지 결정하는 실험 실행의 묶음.
_Avoid_: 실험 묶음, 실험군

**진입 진단**:
champion과 성능을 겨루지 않고 측정 결과에 따라 조건부 실험 트랙을 열거나 닫는 검사.
_Avoid_: 사전 실험, 탐색 실험

**다양성 구성원**:
단독 OOF 성능이 champion보다 낮아도 누출 없는 앙상블 평가를 개선해 후보 풀에 유지하는 모델 실행.
_Avoid_: 약한 모델, 보조 모델

**실험 실행**:
검증된 설정 파일 하나로 시작해 데이터 적재, 특성 생성, 학습, 평가, 산출물 저장을 거쳐 끝나는 MLflow run 하나.
_Avoid_: 학습, MLflow 기록

**피처 계획**:
실험 실행 하나가 학습할 컬럼 전체를, 컬럼 제공자들의 순서 있는 목록으로 선언하고 검증하는 구성 요소.
적용 단계 순서와 placebo 상시 포함을 불변식으로 소유한다.
_Avoid_: 피처 파이프라인, 피처 셋

**컬럼 제공자**:
적용 단계(dataset-wide / row-wise / fold-fit)와 산출 컬럼 이름, 타깃 참조 여부를 정적으로 선언하고 그 컬럼을 만들어내는 피처 계획의 단위.
_Avoid_: 피처 함수, 트랜스포머(fold-fit 한정 용어로만 유지)

**모델 계열**:
`model.kind` 하나로 식별되는 학습기 구현.
파라미터 해석, 시드 적용, early stopping 등 학습 세부를 소유하고 cv 루프에는 예측과 importance만 돌려준다.
_Avoid_: 모델 종류, 알고리즘

**실행 저장소**:
완료된 실험 실행의 사실(params·metrics·tags)과 산출물을 읽고, 뒤늦은 주석(태그·지표 추가)을 남기는 유일한 통로.
MLflow는 이 저장소의 adapter 하나일 뿐이며, 기록 규약의 의미 해석(metric 이름 파싱 등)은 판정 쪽 소관이다.
_Avoid_: run store, MLflow 래퍼

**실행 관찰 기록기**:
실험 실행 하나에 붙어 MLflow 실행의 생성부터 종료까지 수명주기를 소유하고, 진행 기록, 생존 신호, 실행 로그, 오류 기록, 종료 처리를 한곳에서 담당하는 구성 요소.
교차 검증은 이 기록기에 일어난 일을 통지할 뿐 MLflow를 직접 알지 못한다.
_Avoid_: 트래커, 로거, 옵저버

**진행 기록**:
실험 실행의 현재 단계, 시드와 fold 진행률, 단계별 소요 시간과 중간 평가값을 구조화해 남긴 관찰 정보.
_Avoid_: 진행 로그, 상태 메시지

**생존 신호**:
실험 실행이 살아 있음을 보이기 위해 60초마다 남기는 경과 시간 기록.
마지막 신호 시각으로 실행의 중단 여부를 판정한다.
_Avoid_: 하트비트, heartbeat

**실행 로그**:
실험 실행 중 로컬 파일에 이어 쓰고 완료 또는 실패 후 MLflow 산출물로 보존하는 전체 텍스트 기록.
_Avoid_: 콘솔 로그, MLflow 로그

**결과 요약**:
원본 예측 및 특성 중요도 산출물과 함께 저장해 MLflow UI에서 별도 코드 실행 없이 읽는 표와 그림.
_Avoid_: 결과물, 리포트

**요약 생성기**:
실험 실행의 최종 결과와 단계별 소요 시간을 입력받아 결과 요약의 표와 그림을 만드는 구성 요소.
요약 생성이 실패하면 해당 실험 실행은 실패로 처리한다.
_Avoid_: 리포트 생성기

**스테일 실행**:
강제 종료로 종료 처리가 수행되지 못해 MLflow에는 진행 중으로 남았지만 실제 프로세스는 사라진 실험 실행.
마지막 활동 시각과 프로세스 부재를 함께 확인해 판정하고, 정리 절차가 강제 종료 상태로 종료한다.
_Avoid_: 좀비 실행, 고아 실행

**마일스톤 제출**:
baseline과 각 피처 노력이 끝나는 시점에만 수행하는 Kaggle 제출로, public 점수가 CV(OOF) 점수와 같은 방향인지 확인하는 건전성 점검 용도로만 쓴다.
public 점수는 실험 채택 여부의 판단 기준이 아니다.
_Avoid_: 리더보드 확인, 점수 보러 제출

**champion**:
채택된 실험 가운데 현재 최고 성능으로 확정되어, 새 실험의 개선 판정 기준이 되는 run.
_Avoid_: 기준 모델, 최고 기록

**challenger**:
champion과 비교해 개선 판정을 받는 새 실험 run.
판정을 통과해 채택되면 새 champion이 된다.
_Avoid_: 도전 모델

**개선 판정**:
실험 결과의 채택 여부를 정하는 규칙.
특성·단일 모델, 다양성 구성원, 앙상블 세 계열로 나뉘며, 계열별 증거와 문턱은 판정 계약(ADR 0001)을 따른다.
_Avoid_: 성능 비교

**판정 불가**:
증거 부족이나 전제 위반으로 개선 판정을 수행할 수 없는 상태.
미달(판정은 됐으나 탈락)과 구분되는 예외로 보고하며, CLI가 종료 메시지로 번역한다.
_Avoid_: 판정 실패, 판정 에러

**스크리닝**:
고정된 단일 시드로 후보를 빠르게 거르는 1차 판정.
통과는 채택이 아니며 확정 재검증의 대상이 될 자격만 준다.
_Avoid_: 예비 실험, 1차 실험

**대리 스크리닝**:
느린 champion 모델 계열의 공식 스크리닝에 앞서, 빠른 모델 계열의 동일 조건 기준 실행과 후보 실행을 짝지어 특성 후보를 거르는 선별 절차.
대리 모델 안에서 OOF AUC 변화량과 새 특성 중요도를 판정하며, 통과하더라도 champion 모델 계열의 스크리닝과 확정 재검증을 대신하지 않는다.
_Avoid_: LightGBM 검증, 빠른 스크리닝

**약식 검증**:
대량 후보의 순위만 매기기 위해 검증 fold 하나와 가벼운 학습 설정으로 실행하는 선별 절차.
점수는 약식 기준 실행 대비 차이로만 읽으며 후보 선별에만 쓰고, 스크리닝과 확정 재검증 등 champion 판정에는 쓰지 않는다.
_Avoid_: 프리스크리닝, 라이트 검증

**약식 기준 실행**:
champion의 피처 구성을 약식 검증과 같은 조건으로 실행해, 모든 약식 후보 점수의 비교 기준이 되는 실행.
_Avoid_: 약식 베이스라인

**확정 재검증**:
고정된 3시드 평균본으로 채택을 확정하는 2차 판정.
_Avoid_: 파이널 체크

**경계 구간**:
확정 재검증에서 시드 평균본 개선 폭이 확정 문턱 이상, 그 두 배 미만(+0.0001 이상 +0.0002 미만)인 구간.
이 구간에서만 fold 승리(5개 중 3개 이상)를 추가 게이트로 요구하고, 그 외 구간의 fold 승리 수는 보조 증거로 기록만 한다.
_Avoid_: 그레이존, 애매한 구간

**시드 평균본**:
고정된 시드들로 각각 학습한 예측을 평균해 하나로 만든 실행본.
다중 시드 실행의 OOF AUC는 이 평균 예측을 채점한 값이다.
_Avoid_: 시드 앙상블

**후보 풀**:
앙상블 구성 후보로 유지하는 시드 평균본 실행의 장부.
진입과 탈락은 개선 판정의 다양성 계열 규칙을 따른다.
_Avoid_: 모델 리스트

**진입 하한**:
후보 풀 진입의 최소 성능 조건으로, 시드 평균본 OOF AUC가 진입 시점 champion − 0.01 이상이어야 한다.
진입 시점에만 적용하고 champion 갱신 때마다 재심사하지 않는다.
_Avoid_: 컷라인, 최소 점수

**중복 게이트**:
풀 내 최근접 구성원과 OOF 예측의 스피어만 순위 상관이 0.998 이상이면 중복으로 보고 성능이 높은 쪽만 유지하는 판정.
상관은 중복 제거 전용이며, 탈락은 조기 확정이고 교체는 진입 여부를 정하지 않는다.
_Avoid_: 상관 필터, 유사도 체크

**기여 판정**:
표준 평가 앙상블의 OOF AUC에서 해당 구성원을 제외했을 때 AUC가 하락해야 유지하는 판정.
풀이 비어 있으면 묻지 않는다.
_Avoid_: ablation, 기여도 분석

**채택 자격**:
장부(champion·후보 풀)에 오르는 실행이 갖춰야 하는 기록 조건.
3시드 평균본이고, git_dirty가 아니고, 커밋된 folds와 sha256이 일치해야 하며, 검사의 소스는 판정 module 한 곳이다.
_Avoid_: 등록 조건, 제출 자격

**표준 평가 앙상블**:
후보 풀 전체의 순위 평균으로 만든, 구성원의 제외 전후 기여를 재는 기준 앙상블.
_Avoid_: 베이스라인 앙상블

**nested OOF**:
outer fold마다 나머지 fold의 OOF만으로 구성원 선택과 가중치를 정해 만든, 선택 편향 없는 앙상블 OOF.
_Avoid_: 이중 교차검증

**플라시보 피처**:
무작위 값에 실제 열과 같은 결측 패턴을 입혀 모델 입력에 상시 포함하는 잡음 피처.
중요도가 이보다 낮은 피처는 잡음으로 본다.

**fold-fit 피처**:
각 CV fold의 훈련 데이터로만 학습(fit)하고 그 fold의 검증 데이터와 테스트에 적용(transform)하는 피처.
타깃 인코딩처럼 타깃을 참조하는 피처가 검증 라벨을 누수 없이 쓰기 위한 구분이다.
_Avoid_: fold-aware 피처, 폴드 내 피처

**placebo 카나리아**:
플라시보 피처를 후보 피처와 같은 방식으로 타깃 인코딩해 모델 입력에 함께 넣는 누수 자가 검출 컬럼.
구현이 올바르면 중요도가 잡음 수준에 머물고, 평균표에 검증 라벨이 새면 라벨을 암기해 중요도가 치솟는다.
개선 판정의 대상이 아니며, 이 컬럼의 중요도가 플라시보 피처보다 낮아야 해당 run이 유효하다.
_Avoid_: 누수 체크 컬럼

**플라시보 게이트**:
피처의 평균 gain importance를 플라시보 피처의 평균 gain 기준값과 비교해 통과 여부를 정하는 판정 게이트.
카나리아는 기준값 미만이어야 run이 유효하고, 새 피처는 기준값을 넘어야 기여로 인정한다.
기준값이 미기록이면 실패이며, 게이트 규칙의 소스는 판정 module 한 곳이다.
tracking의 경고와 결과 요약의 플라시보 비교는 게이트가 아니라 관찰·표시다.
_Avoid_: importance 필터, 플라시보 체크
