# 규약: 실험 하나 = 설정 파일 하나. 파일 이름이 곧 실험 이름의 접두어.
# 이슈 #106: Lookup-Transformer에서 채택 복원 특성의 한계 기여 결정(복원 블록 실행).
# 기준 exp059_lookup_transformer의 피처 세트(원시 12열 lookup + 파생 6열 PLR)를
# 그대로 두고, CatBoost champion(exp057)에 채택된 복원 블록만 더한다:
# - 제약 결측 재구성(#74): constrained_impute_aux, widths 없음, 9열.
# - XGBoost 조건부 결측 복원(#86): xgb_impute_aux 채택 5열.
# - 복원 행렬 조성(#90): 조성 5열(social_frac, work_frac, leisure_frac,
#   resid_frac, week_total).
# 새 파생 열은 만들지 않는다. 새 열은 전부 lookup_cols 밖이라 PLR 전용 토큰이 된다.
# 정확값 TE는 Lookup 임베딩과의 중복 분리를 위해 별도 실행(exp068)으로 잰다.
# seed 42 스크리닝이 기준 이상이고 새 열 전부 플라시보 상회일 때만 3시드 확정으로 간다.
# Vast.ai RTX A4000 seed 42 스크리닝(run 4094f748)은 기준 대비 +0.000273이고,
# 새 14열 전부 플라시보를 넘어 통과했다. 원격 원본(run 14b600fa)의 dirty 표시는
# 입력 data 심볼릭 링크 하나뿐임을 확인하고 승인된 출처 예외를 반입 기록에 보존했다.
# 클린 3시드 확정(run 2bd55026)은 OOF AUC 0.969098로 exp059 대비 +0.000176,
# 시드 3/3과 fold 5/5에서 개선했고 새 14열 게이트도 모두 통과해 champion으로 채택했다.
name: exp067_lookup_xgb_impute_comps5

data:
  train: data/train.csv
  test: data/test.csv
  sample_submission: data/sample_submission.csv
  folds: artifacts/folds.parquet

features:
  base: raw
  categorical: [gender, stress_level, academic_work_impact]
  providers:
    - kind: derived
      names: [screen_slack, sgw_sum, slack_frac, wk_minus_sgw, wk_other, sgw_frac]
    - kind: constrained_impute_aux
      widths: false
      cols:
        [
          age,
          daily_screen_time_hours,
          social_media_hours,
          gaming_hours,
          work_study_hours,
          sleep_hours,
          notifications_per_day,
          app_opens_per_day,
          weekend_screen_time,
        ]
    - kind: xgb_impute_aux
      cols:
        [
          age,
          daily_screen_time_hours,
          social_media_hours,
          gaming_hours,
          work_study_hours,
          sleep_hours,
          notifications_per_day,
          app_opens_per_day,
          weekend_screen_time,
        ]
      emit:
        [
          daily_screen_time_hours,
          social_media_hours,
          gaming_hours,
          work_study_hours,
          weekend_screen_time,
        ]
      compositions:
        [
          social_frac,
          work_frac,
          leisure_frac,
          resid_frac,
          week_total,
        ]
      cat_cols: [gender, stress_level, academic_work_impact]

model:
  kind: lookup_transformer
  params:
    lookup_cols:
      [
        age,
        gender,
        daily_screen_time_hours,
        social_media_hours,
        gaming_hours,
        work_study_hours,
        sleep_hours,
        notifications_per_day,
        app_opens_per_day,
        weekend_screen_time,
        stress_level,
        academic_work_impact,
      ]
  fit: {}
